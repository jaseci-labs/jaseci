- **Feature: Sixteen sealed native roots and the pass rim**: The cfg builder and the rc-facts solver join the native seal (fourteen roots become sixteen), passes stop carrying a live program handle (a construction-seat rim stamps it), the type evaluator gets its first full seam coverage, and a set of native-lane correctness fixes land (checker tuple narrowing, erased-subscript miscompiles, enum auto() folding, the kwargs sink's self miscount).
