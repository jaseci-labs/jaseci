A `TypeVar`, `ParamSpec` or `TypeVarTuple` glob is now read as the compile-time declaration it is instead of a runtime value, so modules declaring type parameters no longer refuse the native codespace.
