- **Compiler: the graph-shaped compiler (#8744)**: syntax nodes are immutable object-spatial nodes, tree structure and semantic relations are typed edges, the type system is nodes and edges in the same graph, and every pass is a walker. Node fields that passes used to assign (`kid`, `body`, `type`, `sym`, ...) are read through accessors over edges. The jac0 seed tier now lowers the object-spatial subset (`node`/`edge`/`walker`, connect, edge references, `spawn`, `visit`, abilities) so the seed compiler is graph-shaped too. Also fixes the bytecode kernel's quadratic connect (#8743, hash only).
- **Runtime: subtree-fenced `visit:0:`**: front-splicing visits (`visit:0: targets;`) now give walkers true depth-first traversal with enter/exit abilities bracketing each subtree (a node's exit fires when its subtree completes, not when the queue drains). Plain `visit` keeps the legacy queue semantics. The kernel also gained `osp_skip_rest` (suppress the current node's remaining abilities, which is how a walker declines to descend) and a typed-dispatch match cache.
- **Compiler: the tree directs the walk**: `UniNode` carries a `walk_kids` ability triggered on `TreeWalker` (a marker walker every `UniPass` inherits), so the node front-splices its own kids after every walker hook for that node has run. The kernel already orders node abilities after walker abilities, so no scheduling primitive is needed: pass hooks that rewrite `kid` are seen by the expansion, and a walker declines to descend with `osp_skip_rest()`, which suppresses the node's own abilities while still running the exit phase. `UniPass.prune()`/`prune_signal` are removed (use `osp_skip_rest()`), as are the `__jac_post_entry__`/`__jac_post_exit__` kernel callbacks; the three passes that carried an exit tail now declare it as an ordinary ability last in the class body.
- **Compiler: pass hooks are typed OSP abilities**: the `enter_<node>`/`exit_<node>` visitor surface (`accept_enter`/`accept_exit` double dispatch, ~460 generated stubs) is deleted. Passes declare `can enter_x with IfStmt entry { ... }` and the walker kernel dispatches them by trigger type; ability triggers use `isinstance` semantics, so hooks on types with node subclasses carry explicit exact-type guards where the old exact-match behaviour matters. Third-party passes using `def enter_<node>` methods no longer dispatch and must convert to abilities. `jac gen-uni-dispatch` is removed.
- **Compiler: the type system is part of the graph**: every type (`ClassType`, `FunctionType`, `UnionType`, `TypeVarType`, ...) is an object-spatial node, and type composition is typed edges (`TypeOf` from expressions to their types, `BaseOf`/`MroOf`/`TypeArgOf`, `ParamOf`/`ReturnsOf`, `UnionMemberOf`, `BoundOf`, overloads, enum members). Field APIs are preserved through accessors (edges are the source of truth; hot reads memoize per node). Identical class types intern by declaration identity, and the type evaluator is a walker (`expr spawn evaluator` types a subtree on demand).
- **Compiler: graph lifetime and scheduling (WS5)**: `JacProgram.release_module` releases one module's graph by unhooking it from the program root (its kid slot and its `ScopeChild` ties); because in-links are weak (see the light-edges entry below) nothing else in the program holds the tree, so it is garbage-collectable while the program lives (regression-tested with weakrefs, including `release_compile_closure` on a fully compiled module). `release_compile_closure` retains stub (`.pyi`) trees by default: the memoized builtins scope binds into them, and dropping one left those bindings dangling once a collection ran (`print(..., file=)` and `sorted(...)` lost their signatures after enough units in one process). Pass scheduling speaks edge kinds: writers declare what they produce (`SymTabBuildPass` -> `SymOf`/`Defines`/`InScope`, `CFGBuildPass` -> `CfgSucc`/`CfgTrue`/`CfgFalse`, `TypeCheckPass` -> `TypeOf`, `DeclImplMatchPass` -> `ImplOf`, placement -> `PlacedIn`/`DecidedCodespace`) and hard readers require them; every schedule is validated against these declarations when it is built. After a module's pipeline completes, `FreezeStructurePass` locks its tree: structural mutation (`set_kids`/`replace_kid`/`_role_set`) on a frozen node raises, with `thaw_structure()` as the deliberate escape. `set_parent` no longer fabricates a non-kid edge -- synthesized nodes carry a `_ctx_parent` context reference instead. LSP definition/references already run on `Defines`/`Uses` edges via the symbol accessors.
- **Runtime: light edges**: a field-less, directed edge between unpersisted nodes is an adjacency entry (`NodeAnchor.out_light` / `in_light`, keyed by edge class), not an `EdgeArchetype` plus `EdgeAnchor`. Out-links are strong and in-links are weak, so ownership follows connect direction and a fan-in target never keeps its sources alive. `[x ->:T:->]`, `[edge ...]`, `visit`, `disconnect` and `del` see no difference; a node that is saved materializes its light edges into rows. Edges with fields, undirected edges and edges touching a persistent node are unchanged.
- **Language: the simple hop and `del` on an edge set**: `[x ->:T:->]` with one origin, one direction, an edge class and no predicate, filter or chain lowers to a direct adjacency read (`hop`; `hop0` in the seed tier) instead of a planned query, in both the seed and py lowerings. `del [edge x ->:T:->];` is now a statement form: it deletes every edge of the set without materializing it (`clear_edges` / `clear0`); the direction and type follow the reference, `[edge x -->]` deletes all out-edges. Previously `del` of an edge reference did not compile on either tier.
- **Runtime: no per-write hook on transient archetypes**: `Archetype.__setattr__` is gone; persistence dirty-marking is installed on an archetype only when its anchor becomes persistent (`track_writes`, a generated same-named subclass). The walker kernel takes its scope by parameter (no walk-stack lookup after every ability), front-splicing `visit:0:` is O(len(targets)) on a dedicated stack, and the bytecode driver hands the kernel archetypes rather than anchors (no per-ability unwrapping wrapper).
- **Compiler: the scope-parent relation is owned by the child**: a scope's non-kid parent (the builtins scope every module scope hangs under) is a `ScopeParent` edge connected from the child scope to its parent, never the reverse. With the parent as the edge's origin, the long-lived builtins stub would hold a strong link to every module scope ever compiled and pin every compiled tree for the life of the process; that direction grew the payload precompile and a repo-wide `jac check` by a full dependency closure per unit and killed the CI runner. `parent_scope` reads as before.
- **Runtime: one-frame simple hops, connects and visits; light `ScopePrimary`**: `hop0` answers a one-node, one-direction, single-class hop from the adjacency in one frame and returns one entry per edge (the multiplicity `refs` always had; a merged `<-->` or subclass read still deduplicates), `connect0` writes a light edge in one frame, `visit` on a list of archetypes goes straight to the kernel queue, and the walker kernel resolves a node's tag and descriptor once per node. `ScopePrimary` carries no `alias` field: a scope drops the previous primary through `_names_index` instead of scanning every primary edge on every insert, and a symbol is bound to its name once. `make_archetype` gives every node class a docstring before `dataclass()` runs, which stops `inspect.signature` on 557 classes per process, and `release_compile_closure(collect=False)` lets `jac check` skip the full collection after the last file. `jac check` on a small file: 1.67 s to 1.30 s.
- **Compiler: GC thresholds are raised for the length of a compile**: `JacCompiler.compile` sets `gc.set_threshold(100000, 50, 100)` at its outermost entry and restores the previous thresholds when it returns; `jac check` keeps them for the process since it exits right after. The compiler's tree is many small tracked objects, and the default `(2000, 10, 10)` schedule spent 0.57 s of a 1.2 s check in 602 young and 5 full collections; with the boost it is 14 young passes and 0.06 s. `JAC_GC_NO_COMPILE_BOOST=1` restores the default schedule. `jac check` on a small file: 1.30 s to 0.80 s; the chess example 2.08 s to 1.39 s in-process.
- **Retired**: `jac gen-uni-dispatch` and the generated visitor surface, `AstTool.pass_template` (emitted templates for the deleted visitor dispatch), `UniPass.term_signal`, and the role-name string bridge.
- **Compiler: `JAC_COMPILER_DIGEST_PIN`**: a developer knob that freezes the compiler-tree digest folded into every JIR's environment key, so an edit to the compiler recompiles only the modules whose own source changed. Unset, behaviour is unchanged.
- **CLI: one fork work pool for `jac test`, `jac check`, `jac fmt` and the precompiler**: `jaclang.cli.workpool.WorkPool` replaces the test runner's private `ForkPool` and `jac fmt`'s `ProcessPoolExecutor`; the parent hands file paths to forked workers over pipes (one JSON line per request and record), crashes are reported per file and the slot respawned, and a worker retires at an RSS cap (`JAC_<CMD>_RECYCLE_MB`) so a job never has to `gc.collect()` after every item. `jac check` workers keep their stub prelude across the files they serve, and the check diagnostics cross the wire as `{text, pretty, code, category, mod_path}`. Every command takes `--jobs/-j` (a count, `auto`, or `0` for serial) with the precedence `--jobs` > `JAC_<CMD>_JOBS` > `JAC_JOBS` > `[dev] <cmd>_jobs` > `[dev] jobs` > auto (one worker per core, memory-budgeted); `dist/precompile_bytecode.jac` takes `--jobs=N`. `[dev] test_jobs` keeps its meaning (an empty value now means auto). Serial mode runs the same job inline in the parent, so one code path does the work in both modes; platforms without `os.fork` always run inline.
- **Compiler: the stub catalog**: the type checker no longer parses typeshed. A build step (`payload stubcat`, run by mkpayload after the precompile) loads every stdlib stub the interpreter can see plus jac's own `.pyi`, forces every declaration through the type evaluator and writes the result as one flat, offset-addressed catalog (`stubcat.bin`, ~5 MB for 596 modules): module, scope, symbol and type records with cross references by id, the merged `builtins` scope (jac_builtins, builtin_overrides, dom_types and the typing ambient names adopted at build time) and the ambient table. The reader maps it and materializes records on first touch, so a compile pays for the stub declarations it uses rather than the 61k nodes and 11.7k symbols the old path built per process: `jac check` on a small file 0.73 s to 0.17 s and 277 MB to 72 MB, the chess example 1.35 s to 0.62 s; diagnostics are unchanged. The catalog is a page-aligned region of the fused binary (`[launcher][stubcat][descriptor][payload][trailer]`, found from the payload's start so the v1 trailer and every consumer of it are untouched; the launcher exports `JAC_STUBCAT_OFF/LEN` like the `.jab` overlay) with `_precompiled/stubcat.bin` in the payload as the file fallback; a source checkout builds its own into the JIR cache on the first user compile, keyed by the typeshed pin, the interpreter and a digest of the type-system sources. `Symbol.provider` is the seam: catalog symbols answer their type, definition and property classification from records, and the evaluator never reads their syntax; star-import re-exports (`from .tasks import *` in `asyncio`) and class metaclasses (`ctypes` array types) are resolved at build time and stored on the records, since the evaluator resolved both from syntax before. Typeshed resolves through the catalog alone once one is active; the `.pyi` source path remains for the catalog writer, the compiler's own bootstrap and PEP 561 project stub packages. `release_compile_closure` loses its `keep_stubs` parameter: catalog-served stubs never enter a program hub, and a `.pyi` a process does parse from source (a project stub package, or the bootstrap and the catalog writer, which run source-only) is retained across a release as before. `--skip-stubcat` / `-Dskip-stubcat` skips the build for fast link validation, and `JAC_STUBCAT_FILE` points a process at an explicit catalog.
- **Compiler: absolute imports inside typeshed follow Python semantics**: a `from bz2 import *` written in `compression/bz2.pyi` (Python 3.14's re-export package) resolved to the file itself under the sibling-first rule, and because a stub resolved its absorb imports before entering the program hub, it re-parsed itself once per recursion level until memory ran out (31 GB observed). Importers under `vendor/typeshed` now skip the sibling and self-tree candidates, and a stub module is published to the hub before its absorb imports are resolved, so any remaining cycle terminates.
