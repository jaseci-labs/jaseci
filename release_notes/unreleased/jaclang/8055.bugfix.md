- **Fix: config reads no longer mutate the loaded config**: `get_kubernetes_config()` derived keys stay out of the cached config, and asking for a different project's config now errors instead of silently returning the first project's.
