- A `comptime import` now takes its dependency as a tree instead of a cached interface (which carries no values), so warm `jac check` runs no longer report false "not known at compile time" errors on comptime bindings over imported values; the dependency also joins the importer's comptime inputs, so editing an imported comptime value re-analyzes the importer even when the interface is unchanged.
