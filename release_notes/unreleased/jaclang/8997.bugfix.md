- **Client lifecycle cleanup**: Synchronous client `can with entry` blocks can return cleanup functions without the misleading ignored-return warning. Solid now registers these functions for cleanup on reactive changes and disposal, matching the lifecycle pattern supported by React and Preact. JacYac uses lifecycle blocks for subscriptions, observers, and timers, and its live source clips resolve current anchors and fit the mobile showcase layout.
