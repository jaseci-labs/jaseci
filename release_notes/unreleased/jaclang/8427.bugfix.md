- **Setting `[scale.monitoring] namespace = ""` no longer stops the server from starting** (#8057): serving a project whose monitoring namespace was left explicitly empty failed at startup with `NameError: name 'sanitize_prometheus_namespace' is not defined`, even when monitoring was disabled, instead of deriving the Prometheus namespace from the Kubernetes namespace. The server now starts, and `namespace = "jac-shop-prod"` under `[scale.kubernetes]` yields the Prometheus namespace `jac_shop_prod`.
