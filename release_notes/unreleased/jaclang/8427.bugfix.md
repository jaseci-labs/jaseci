- **Setting `[scale.monitoring] namespace = ""` no longer crashes** (#8057): reading the monitoring config with the namespace left explicitly empty raised `NameError: name 'sanitize_prometheus_namespace' is not defined` instead of falling back to the sanitized Kubernetes namespace it was meant to derive. The fallback now works, so `namespace = "jac-shop-prod"` under `[scale.kubernetes]` yields the Prometheus namespace `jac_shop_prod`.
