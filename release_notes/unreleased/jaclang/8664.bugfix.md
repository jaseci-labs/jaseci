- **Docs and tests: group sharing is the O(1) way to share with an audience**: `allow_group(obj, group_id, level)` plus an edge-based membership is now documented in the `jac-sv-multi-user` skill beside the per-root `allow_root` it replaces for audiences, and a regression test asserts the cost invariant on real serialized rows - joining and leaving a group leave every content row byte-identical while the member's visibility flips. Closes #8460.
