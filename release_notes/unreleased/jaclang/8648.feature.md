- **Feature: the fused native prefix reaches seat four, and the seat rule names itself**: With the cross-module devirtualization fix in (#8645), `DeclImplMatchPass` and `SemanticAnalysisPass` both run natively inside the crossing with no further change, sealing green on the first attempt with every canary passing including prefix parity under the symbol-table comparator. `PREFIX_NATIVE_FRONTIER` moves from 2 to 4. That both passed first try is the point: every pass in the chain inherits `UniPass`, so every one of them had been calling base-declared hooks that bound to empty base bodies, and the frontier had sat at one since #8323 not because six sealed passes each carried a separate defect but because none of them could run at all. Seats five and six do not follow, for a structural reason rather than a bug. The crossing constructs a pass as `Pass(ir_in=mod)` and nothing else, because inside the native unit `PassRim` is erased and no `JacProgram` exists to hand it, and that spelling only type-checks for a `class` archetype where `has prog` lowers to a bare annotation and generates no constructor parameter. An `obj` subclass of the same base materialises `prog` as required instead, so `run_prefix_passes` stops compiling with E1050, and `SemDefMatchPass` and `CFGBuildPass` are the two `obj` passes in the claimed prefix. At runtime the two kinds are indistinguishable, since every pass is a dataclass whose `__init__` takes `ir_in` and never takes `prog` (the metaclass pops it first), so nothing caught an `obj` seat until the seal built the materializer minutes later and failed with an error naming a parameter rather than the archetype kind behind it. A new check parses each claimed seat's own source and asserts its archetype kind, so the next person to move the frontier is told which pass and why in seconds. Seat seven stays out of reach for an unrelated reason worth recording: the claimed-count cap is spelled as the literal 6 in three places that all duplicate the length of `PREFIX_IR_GEN_NO_CGEN`.
