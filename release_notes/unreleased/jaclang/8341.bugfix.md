- **Fix: a native unit's demotion verdicts now bind every emission in it, import cycles included**: a module compiled while its callee module sat inflight above it in an import cycle could not learn the callee's demotion verdicts, assumed every callable lowers, and baked live calls into IR that the callee's later rename-and-pop turned into dangling declarations -- the sealed `libjac_module_codegen_pass.so` dying at dlopen with `undefined symbol: pretty_print_source_location`, in exactly the root orders whose walk betrayed the caller. The emission side now records every callable declared live under an unknowable verdict, and the link phase -- where every verdict is resolvable -- re-validates the record before consuming a module's IR, re-emitting the module loudly when a landed verdict disagrees (E5028 names the module that cannot converge, and cached dependency IR carries the record so a warm cache cannot serve an outlawed emission). With the ordering consistent, the pass base chain's closure work re-lands: `transform.jac` imports `CodeLocInfo` from its declaring module so the base chain joins every pass root's closure, the inherited-slot linearization repair (`extended_mro` with declaring-module provenance) clears `UniPass.transform`/`traverse`/`__post_init__` and the jsx guard's inherited read, pass construction in a native root lowers again after the rim's driver-side `prog` keyword stopped being demanded where the rim base is erased, and the per-root transform waiver asymmetry -- an artifact of the ordering bug -- collapses to one uniform set.
