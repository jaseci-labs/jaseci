- **Faster `jac seal`, and every CLI command exits promptly**: sealing one module used to compile its whole native closure twice, because the demotion verdict was not reproducible and a refusal bought a second opinion. With the cache-off intent now held for the compile region, one compile settles it. Separately, the CLI returned an exit code up the dispatcher and let CPython walk a multi-gigabyte object graph on the way out, worth 12.4 seconds of a 42 second run on its own; `run_selected_command` is the single point where every command ends and nothing calls it in-process, so it now runs the exit hooks, flushes both streams and exits without the teardown walk (`JAC_SLOW_EXIT=1` restores the old behaviour). Two duplicated passes over 39 MB of LLVM IR text in `seal_one_root` are gone with them. Sealing `compiler/frontend/unitree.jac` goes from 66.2 to 27.2 seconds with one closure compile instead of two and peak memory from 5.40 to 3.05 GB, reporting the same 42 demotions with the same reasons.
