- **The type checker's type-system imports move to module scope, and the final pass seal is refused with its mechanisms named**: every body-local `import from jaclang.compiler.type_system { types }` in `type_checker_pass` was a package-submodule import the native lane refuses, and the earlier attempt recorded them as resisting module-scope conversion because the checker resolved body-local imports more widely than module-scoped ones. Re-tested with the type system linked, the scoping rule holds: all twenty-four sites collapse into three import blocks in the pass header, and the `Python module import 'jaclang.compiler.type_system'` family leaves the coverage entirely. The seal itself is refused. `type_checker_pass` demotes 46 of its 48 methods, leaving only the `_lambda_depth` counter pair native, because the pass is a thin visitor over `TypeEvaluator`: `evaluator` is stamped onto the pass in `before_pass` rather than declared, so all 134 reads cross the Python-base boundary, and the type behind them lives in the largest module the native lane has never compiled. The closure also produces no LLVM IR, because `type_system/operations.jac` carries `TypeEvaluator` in four signatures through a cycle-breaking `import type from`; that name resolves to `UnknownType`, and the signature resolver erases a known-but-unlowerable type to an opaque pointer while sending an unknown one to a module-level error that the per-callable demotion window can never absorb.
