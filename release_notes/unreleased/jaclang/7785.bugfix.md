- **Fix: wrapped operator chains indent their continuation lines**: An over-width operator chain now hangs its continuations one indent level under the line that opens it (`out += a` continuing as `+ b`), instead of flush with the statement where they read as sibling statements. Inside parentheses the wrapper keeps owning the single indent level, so `if (...)` conditions and other parenthesized chains do not double-indent.
