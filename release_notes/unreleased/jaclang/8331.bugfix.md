- **Fix: `isinstance(x, tuple)` narrows again in both branches**: the checker read the bare class `tuple` as a named tuple-of-classes table (the `isinstance(x, TABLE)` form), and the empty table silenced the guard entirely, so `x` stayed the full union inside the check and `tuple` was never subtracted in else/elif branches. The table form now requires a tuple value, so the bare class narrows like any other class.
