- **Fix: multi-statement lambda hoist placement on the Python target**: Multi-statement lambdas used as decorator arguments, default parameter values, `has` field defaults, or enum decorators no longer fail at runtime with `NameError: name '__jac_lambda_N' is not defined`. PyAST codegen now drains hoisted helper defs into the correct enclosing scope before the reference is evaluated; class-body defaults capture the function object at definition time. Adds compile-time diagnostic `E5098` when a hoist cannot be placed safely, plus regression fixtures and client/native parity probes.
