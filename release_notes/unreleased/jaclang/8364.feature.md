- **The fused prefix's construction gate burns down**: the four waived abort stubs reachable through a native pass construction each clear by their announced mechanism, none by waiver. `Transform.ice` becomes the message-builder half of the internal-error guard (its refusal was constructing the exception as a value, never the raise -- callers read `raise RuntimeError(self.ice(...))` now, 97 sites swept, byte-identical diagnostics on both lanes); cancellation crosses as a scalar (`cancel_requested`, stamped at construction; the event poll stays with the driver and the Python loader, and jac-lane mid-pass cancellation coarsens -- namedly -- to the between-pass granularity the schedule already polls at); and the two `type[T]`-generic `UniPass` forwarding shims retire outright for the `UniNode` methods they wrapped, their six callers all outside the sealed closure. `BASE_CHAIN_ARGUED` empties and retires exactly as its own comment promised, `_TRANSFORM_SEAM_WAIVERS` shrinks to `Alert.pretty_print` alone, uni_pass contributes zero waiver entries, and every two-way coverage re-pins to the measured state -- clearing the way for `parse_and_run_prefix` to construct and run passes natively. Also pinned: the methods-side twin of the free-function trust hole (a consumer reading a demoted jaclang-internal accessor refuses honestly and nothing dangles).
