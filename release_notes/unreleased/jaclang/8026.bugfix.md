- **Service-to-service calls now shed load from a failing dependency much sooner**: every failed request attempt counts toward the circuit breaker instead of only the last attempt of a retry loop, and once the cooldown ends a single trial call is admitted rather than the whole waiting burst.
