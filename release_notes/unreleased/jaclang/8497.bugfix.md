- **Fix: Clearer diagnostic for bare tuple loop targets**: `for n, c in items` now reports "tuple targets must be parenthesized" with a help line showing the correct form (`for (n, c) in items { ... }`) instead of the confusing "Expected 'in', got ','". Applies to both `for` statements and comprehensions.
