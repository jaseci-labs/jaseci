- **CLI: `jac --help --all`, and three verbs step out of hiding**: the hidden long tail of #7255 (`seal`, `dot`, `completions`, the `gen-*` generators) finally has a front door. `jac --help` now ends with a count of the commands it does not list and the flag that lists them, and `jac --help --all` renders them under their own `Hidden:` section without promoting them to the visible surface or the verb budget. `nacompile`, `browse`, and `precommit` are no longer hidden: they appear in `jac --help` under their groups, which brings the visible surface to the budget of 26.
