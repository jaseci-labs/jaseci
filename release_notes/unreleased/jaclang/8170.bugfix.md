- **Fix: the --scale bundle declares its members and pods boot from the declaration** (#8105): `pack_jab` now writes a `manifest.json` at the .jab root naming every shipped member and the resolved file for every declared service (including the implicit `main`), and the pod entrypoint resolves its module from it, replacing the `find` that picked same-named strays nondeterministically. Parked, missing, or ambiguous service modules now fail the pack naming the file and the setting, instead of crash-looping the fleet; the entry-module guard also covers baseline exclusions, a project top-level `manifest.json` is refused as reserved, and `JAC_SV_FILE` joins the protected pod env keys.
