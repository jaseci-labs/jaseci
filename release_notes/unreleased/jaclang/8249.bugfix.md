Retired the last two bare dotted module imports in the compiler (`compiler/symbol_utils.jac` and `jac0core/const_fold.jac`), which the placement scanner read as non-native imports and which kept both modules out of every native unit. `fold_const` now spells its return type, so the module it lives in keeps its IR instead of refusing on an unparameterized `tuple` the moment it becomes placeable.
