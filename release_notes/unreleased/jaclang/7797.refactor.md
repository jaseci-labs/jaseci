- **Refactor: The ecmascript backends and primitive emitter tables are type-checked again**: The Solid backend, the JavaScript unparser, the ESTree node definitions, and both primitive emitter tables carried long-standing type-check exemptions. Clearing them corrected real declaration errors: a fragment view tag demanded a source node where all sixteen other view types accept none, a block statement's body referred to an undefined name so its element type never resolved, the JavaScript code generator used two indentation fields it never declared, and the Solid tag-name helper rejected the optional member-expression object it is routinely handed. The generated JavaScript is unchanged; these were mismatches between what the code did and what it declared.
