- **Scale: a written rig protocol for validating scale changes**: `jac/jaclang/scale/TESTING.md` records how to build a binary that serves the branch under test, how to prove it does before trusting a result, which kind of test a scale change earns (end to end and integration first, no mocks, a regression test that fails before the fix), and the ladder from formatter to real deploy that a scale pull request walks.
