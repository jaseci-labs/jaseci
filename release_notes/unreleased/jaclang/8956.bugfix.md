- `Root` and `GenericEdge` now alias their pre-#8682 module path (`jaclang.jac0core.archetype`), so anchors persisted before that package move deserialize again instead of being dropped on read. `Serializer.register_alias` also registers the class it's given immediately if it isn't registered yet, so an alias to a not-yet-registered `jaclang`-internal class no longer silently fails to resolve later.
