- **`jac guide jac-comptime` and comptime diagnostics that point at it**: a bundled guide covers `comptime` bindings, `comptime if`/`for`/`assert`, comptime defs and imports, comptime parameters on defs and archetypes, the `jaclang.comptime` intrinsics, per-tier erasure, and the E0033/E0108/E0109 diagnostics; `jac check` now links those three codes to the guide, the guide sits in the Language group of `jac guide` and the MCP guide index, and the reference docs (keyword table, syntax cheatsheet, Types & Values, Functions & Objects, Diagnostics, compiler internals) describe or link the feature.
