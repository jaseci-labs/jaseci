- **Fix: `QHop.repr` narrows its node constraint with positive isinstance checks**: `QHop.nd` is `type | tuple | None`, and the repr's else branch relied on negative isinstance narrowing that the checker does not apply, so `jac-check` on main failed with E1099 at the `.__name__` access. The branch chain now narrows positively, tuple then type then None, with identical runtime behavior.
