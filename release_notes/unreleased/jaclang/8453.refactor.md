- **Scale: the imports and the helper `#8415` orphaned are gone**: deleting the deploy helpers the unified Kubernetes path had stopped calling removed only the one import that pointed at a deleted name. Six bindings in `kubernetes_utils` (`base64`, `tarfile`, `requests`, `urllib3`, `RequestException`, `SSLError`) and one in `kubernetes_target` (`ensure_namespace_exists`, called only from `target.jac`, which imports it itself) were left pointing at things their own module never uses. `requests` is the one worth naming: the only occurrences of that token in the module are `pvc.spec.resources.requests`, a dict key on the PVC body rather than the module. `read_project_env` goes with them, having lost its last caller when `load_env_variables` was deleted and having none anywhere else in the tree; `project_env_path` and `load_project_env` stay, the latter live at `plugin.jac:265`. Pure deletion, no behavior change.
