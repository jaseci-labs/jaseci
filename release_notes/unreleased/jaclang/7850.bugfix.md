- **Fix: float/int64 bit-reinterpretation in native-compiled code**: `__float_bits` and `__bits_float` (reinterpret a `float`'s raw bits as an `int` and back) now lower correctly in the native backend regardless of how the call is resolved, so bit-level float manipulation no longer silently falls through to an unrecognized call and produces a wrong result.
