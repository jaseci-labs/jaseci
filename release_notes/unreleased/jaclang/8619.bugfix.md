- **Fix: generated CEF desktop host escapes the `exit` keyword**: `cef_desktop_target` emitted bare `sys.exit(...)` calls into the host source it generates, so `jac nacompile` of the CEF host failed with E0013 once `exit` became a keyword. The four emitted call sites now use the escaped attribute.
