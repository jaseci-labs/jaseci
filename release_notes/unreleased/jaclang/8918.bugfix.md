- **Client builds work again**: #8825 replaced `program.analysis.analyses_run` with an `AnalysisLedger` whose surface is `provided`/`provides`/`mark`/`clear`, and migrated every call site except `_client_target_module` in `client/impl/client_bundle.impl.jac`, which still called the old dict's `.get()`. That function is on the client build path, so it raised `AttributeError: 'AnalysisLedger' object has no attribute 'get'` for every client project: `jac build` reported `Build failed`, the pwa target reported `Target 'pwa' failed`, and serving a base-route app reported `Error rendering base route app` and then 500'd its routes. The call now goes through `provides`, as the other call sites already do.
