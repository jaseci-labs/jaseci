### Changes

- refactor(parser): hoist repeated TokenKind enumerations into named frozensets and predicate methods; collapse guarded expect_any calls to advance()
- refactor(native): extract shared `_write_ehdr`/`_write_phdr` ELF writer helpers, removing 14 duplicated pack blocks
