- **Perf: sealed typeshed image for stub IR**: the type checker resolves vendored stdlib `.pyi` stubs from a build-time sealed JIR bundle keyed by module fullname, avoiding per-load source re-hash and live compile on cache hits. Bootstrap stubs (`builtins`, `typing`, local `*_builtins`) and `_typeshed` stay on the live path so overload and Callable inference keep source-backed tokens.
