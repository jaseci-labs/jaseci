- **byLLM: usage/cost now reaches every `by llm()` invocation shape**: previously only `stream=True, logging=True` calls surfaced token usage and cost; every other shape computed the same data and discarded it. All shapes now get the same visibility through the `tokens` field on `register_agent_callback`'s telemetry record.
