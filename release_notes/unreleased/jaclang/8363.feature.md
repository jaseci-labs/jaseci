- **The codeinfo hold is spent, the pass spine seals into every closure, and the checker declares its evaluator**: `PLACEMENT_INFERENCE_HELD` becomes lane-scoped -- its sole entry held `jac0core/codeinfo.jac` out of native inference, and through it `transform.jac`, `uni_pass.jac` and `module_codegen_pass.jac` out of every native unit. The compiler's own roots (`jac0core/`, `compiler/`; `PLACEMENT_HOLD_EXEMPT_ROOTS`) are now exempt from the hold, while user-facing JIT inference keeps it: the first full CI run showed the entry was also the one gate keeping the jaclang-internal import universe off the default-native path for user code, and dropping it globally split walker class identity across byllm and scale (`Invalid walker object`, sv envelopes losing `reports`). With the compiler lane exempt the spine joins all sixteen sealed closures at exactly the price the hold recorded (codeinfo's 13 seams, waived by name as filesystem/config/placement machinery, while uni_pass's base-chain field wall turned out to be down everywhere -- the composed measurement read `__post_init__`/`transform`/`traverse` lowering in every root, so uni_pass contributes no waiver at all; the two transform handles the hold priced cleared outright as well -- the cancel scalar and `ice` as a message-builder landed with the prefix work, so `transform.jac` joins with no waiver at all) and pays out across the front: `UniNode.gen.__get__` lowers everywhere and its waiver is deleted alongside `SymTabBuildPass.exit_global_vars`, `SemanticAnalysisPass.enter_has_var` and four jcir interop entries; `capability_check_pass` drops five own seams to one (the pass-tier crossing, whose seal is refused by name -- the seal's abort-stub linkage check found the live `NativeCapabilityCheckPass.transform` linking that stub, so the root sits in `NATIVE_SEAL_REFUSED_ROOTS`); `ownership_check_pass` reads 26 own seams to 12 with the `emit` family empty; `static_analysis_pass` 6 to 5; and a native root constructs a pass over the linked base chain for the first time (the rung-3 gate, pinned open). The evaluator/checker joint flip designed in #8328 lands at its pre-written numbers: `has evaluator: TypeEvaluator;` on the `class`-tier checker plus the emptied entry gives `type_checker_pass.jac` IR over the 28-module closure containing `type_evaluator.jac`, 14 seams across 8 causes against 43, with all 243 checker diagnostics unchanged. The grown closures flushed out five latent native defects only the seal's canary tiers and the composed measurement could see, each fixed: a `:priv` function advertised under its unmangled name (`hashlib.new`), the transitive-source walk splitting one module into per-base aliases so glob inits ran before their dependencies (a `__jac_shared_init` segfault), clib declarations (`EVP_*`) polluting every layout's advertised surface, the console gutter's old-style `%` formatting -- now plain padding with byte-identical output, which lowers `pretty_print_source_location`, `format_related_spans` and `Alert.pretty_print` in every unit; and the checker's module-file probe answering case-blind on macOS/Windows (`Path.exists`), which resolved codeinfo's `ExecutionEngine` *class* to the `executionengine.jac` *module* and erased two engine-handle fields in every unit linking codeinfo -- the probe now requires the exact on-disk name. Pooled seal green over all sixteen roots with every canary passing, and the sealed image compiles and runs the chess corpus end-to-end.
