- **Feature: `jac cache` command**: `jac cache ls` lists every cache store with size, schema version, and prune policy; `jac cache prune` runs the retention policies; `jac cache purge [name] [--all]` clears one store or all of them. `jac purge` is now registry-driven and actually reaches every store (`models/` is kept unless `--all`).
