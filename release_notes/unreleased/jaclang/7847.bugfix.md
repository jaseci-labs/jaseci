- **Fix: corrupted strings in mixed-type lists**: in natively compiled code, storing a computed string (a slice, a concatenation, or a passed-in value) into a list that also holds other types (for example `[count, name]`) could read back as garbage or an empty string, and a program built on that pattern could crash. Such strings are now kept alive correctly and round-trip intact.
