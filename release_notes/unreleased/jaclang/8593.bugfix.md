- **Static analysis: W2003 unused-variable now fires for if/else dual assignment**: a variable assigned in both branches of an `if/else` and never read escaped the "defined but never used" warning because assignment targets were counted as uses (the ternary equivalent warned). The check now distinguishes genuine reads from write-position rebinds, so dead stores warn consistently across all spelling variants. Closes #8535.
