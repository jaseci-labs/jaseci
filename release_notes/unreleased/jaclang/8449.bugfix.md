- **Large uploads through the Jac Scale gateway no longer balloon its memory**: a request body is now streamed to the service that handles it instead of being held whole in the gateway, so a 90 MiB upload leaves the gateway's memory flat where it previously added around 187 MiB for every upload in flight.
