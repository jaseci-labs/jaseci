- Dropped the 43 docstring-only `__init__.jac` package markers under `jaclang/`; a package directory without a marker resolves as a namespace package, so nothing changes for imports. The native placement verdict now refuses a module with nothing to lower, so an empty marker is never a native unit. The checker's resolver now treats a directory without a marker as a namespace package, so `import from pkg { submodule }` resolves and go-to-definition lands on the submodule.
