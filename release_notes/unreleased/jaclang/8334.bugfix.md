- **Fix: a deploy that is missing the `env` capability says so**: the deploy realizer no longer imports `dotenv` at module scope, so `jac start --scale` reports the capability to sync instead of failing at an import line before any deploy code runs, and that import now sits inside the guard that turns realizer failures into a `Deploy failed` diagnostic. The three copies of the project `.env` load collapse into `jaclang/scale/config/project_env.jac`, which owns the path, the capability and both reads. Behavior is unchanged, including keys declared in `.env` without a value.
