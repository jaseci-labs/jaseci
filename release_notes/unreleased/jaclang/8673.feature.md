- **Feature: the seal's parity gate sees what the sem-def pass writes, and the claim cap reads its own pin**: The symbol-table comparator the seal runs over the fused prefix compared each scope's name set, overload and implicit-rebind keys, `kid_scope` fan-out and parenting, and every name atom's symbol identity with its defn and use counts, but not `semstr` -- the one field `SemDefMatchPass` exists to write. Admitting that pass to the crossing therefore produced an artifact that linked, loaded, and passed every canary while silently dropping every `sem` declaration in the program: eight semstrings on the byllm enum fixture became zero, and the only thing that noticed was an unrelated integration test. The comparator now renders each scope's semstrings, and the seal's parity corpus grows a `sem` declaration so there is something to compare; the pair is proved red against the exact artifact the old gate called green, with the refusal naming the scope, the symbol and the missing value. Separately, the claimed-count cap was spelled as the literal 6 in three places -- the parse entry, the seal canary's call, and the canary's pass table -- all duplicating the length of `PREFIX_IR_GEN_NO_CGEN`, which is seven, so the last claimed seat was unreachable because of the duplication rather than the pass; all three now read the pin. The frontier stays at four: seat five drops semstrings as described, seat six faults on a reference-count operation against `UniCFGNode`'s uninitialised `postinit` fields for roughly one source in ten, and seat seven cannot be admitted ahead of six.
