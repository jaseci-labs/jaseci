- **Scale: two imports nothing uses are gone**: `ApiException` in `admin/k8s_ops.jac` and `BaseConfig` in `deploy/target/factory.jac`. Both modules are imported from elsewhere for other names, and neither of these two is among them, so removing them changes nothing a consumer can see. Pure deletion, no behavior change.
