- **Scale: three imports nothing in their module uses are gone**: `ApiException` in `admin/k8s_ops.jac`, `BaseConfig` in `deploy/target/factory.jac`, and the `_resolve_client_ip as client_ip` binding in `identity/audit.jac`. The last is worth naming because it was the only edge from the identity audit module to the rate-limit middleware, so removing it drops a dependency between two unrelated subsystems rather than only a name. Pure deletion, no behavior change.
