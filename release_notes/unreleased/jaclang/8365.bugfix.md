- **Fix: the native verdict re-check consults only the externs the caller's IR references, and answers cached verdicts from disk**: the verdict-before-emission re-validation recorded every callable a callee defines while the import walk forward-declares that whole surface into the caller, so in the compiler's cyclic core nearly every cycle-window caller conflicted with any callee that demotes anything -- each false conflict an invalidate-plus-recompile -- and every fresh program answered callee verdicts by compiling the callee unit in-process, which turned the 761-unit precompile from 9 into 26 minutes on CI and left the step wedged after its Done banner. Pending records are now pruned to the symbols the caller's IR both declares and uses (a bare forward declaration cannot dangle at link, and any baked-in call leaves a symbol use, so no real betrayal is hidden), `native_unit_demotions` reads the callee's content-keyed `ir_meta` instead of compiling when caching is enabled, and the persisted record is the pruned one -- so a warm-cache unit compile validates nothing while the genuine betrayed callers still re-emit and cascade exactly as before.
