- **Fix: Heap tuple boxes now own their fields**: A boxed tuple retains the managed fields it is handed and releases them from a generated destructor, so unpacking one, overwriting a bound target, or binding a single field out of it no longer leaks; tuple unpack releases what the target slot held for value tuples too, moves elements it already owns, handles optional targets, and optional returns hand back a +1 reference like every other reference.
