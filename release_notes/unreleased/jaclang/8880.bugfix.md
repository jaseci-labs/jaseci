- **A comptime fold keys on the content it folded from**: a module that folds a value out of another module at compile time now records the producers its evaluation actually read (transitively) and invalidates when their content moves, instead of serving the value baked in by an earlier compile; producers are also ingested with full trees, so a fold no longer fails with `E0033` once the producer is served from its cache.
