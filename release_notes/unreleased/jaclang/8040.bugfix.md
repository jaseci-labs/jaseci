- **Fix: cl tests with only npm imports**: Client tests run under bun in a sealed temp dir with no `node_modules`, but npm import stubbing only ran for modules that also imported `@jac/runtime`. A client test whose only foreign dependency was an npm package kept its bare specifier and bun failed the whole harness with `Cannot find package`. npm stubbing now runs for every client test; staging the client runtime stays conditional on the module actually talking to a backend.
