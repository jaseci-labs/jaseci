- **Native build caching across links**: The osp/fmt runtime kernels are now parsed and verified once per process and linked from a preserved module clone instead of being re-parsed from IR text on every native build. Static archive symbol indexes and member tables are memoized content-keyed, with per-member undefined-symbol lists cached across selections. No change to emitted binaries.
