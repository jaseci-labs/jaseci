- **Native generator expressions iterate tuple sources** (#8318): native comprehensions convert tuple inputs to typed sequences before iteration. Calls such as `sum((x for k in ("a", "b", "c")))` no longer return `0`, and unsupported iterable or target shapes report E5092 instead of producing an empty result.
