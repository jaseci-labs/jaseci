- **Native generator expressions consume tuple sources** (#8318): tuple-backed comprehensions now use the same checked, typed sequence conversion as native `for` loops. Calls such as `sum((x for k in ("a", "b", "c")))` visit every tuple element instead of silently receiving an empty generator and returning `0`; tuples with incompatible native element storage produce an explicit E5092 diagnostic.
