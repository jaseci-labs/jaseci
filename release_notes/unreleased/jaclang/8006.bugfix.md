- **Fix: Reserved gateway route segments are a single source of truth**: a service named `cl` is now rejected at config time instead of deploying and having its traffic intercepted; user-supplied route prefixes are validated and duplicate prefixes rejected.
