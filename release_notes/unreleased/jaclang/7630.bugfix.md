- **Repeated serializer warnings now report how often they happened**: instead of a single line per problem for the whole process, each recurring deserialization warning re-emits on a 1/10/100/1000 ladder with its running occurrence count, so a one-off drift and a millionfold drift are no longer indistinguishable in the logs.
