- **jac check no longer fails order-dependently on files it never touched**: a long-lived check worker could start on compiler-tree files with typeshed parsed as stub trees and then flip to the stub catalog for its first application file, after which Optional narrowing and generic binding silently stopped working inside impl bodies (E1099 / E1053 on lines that pass in isolation). The typeshed lens is now decided up front and stays fixed for the process, and None recognition compares class records by canonical identity.
