- **Fix: jac2js emits `@jac/runtime` imports at codegen on every JS path**: JSX lowering and remote spawn now inject their required `@jac/runtime` imports (`__jacJsx`, `__jacSpawn`) through the ESTree import mechanism inside `EsastGenPass`, so `mod.gen.js` is emission-ready without consumer-side regex finalizers. `js_runtime_globals.jac` and the `finalize_js_for_emission` / `prepend_active_runtime_globals` passes are removed from the CLI, MCP, bundle builder, HMR, eject, test runner, and publisher; npm publishing remaps `@jac/runtime` to the configured runtime package exactly once instead of prepending a duplicate import, and the runtime tarball no longer self-imports `@jac/runtime`. Fixes #7540.
