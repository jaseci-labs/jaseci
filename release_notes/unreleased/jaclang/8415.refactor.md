- **Scale: the deploy helpers `#7828` orphaned are gone, along with the import left pointing at one**: Unifying the Kubernetes deploy path deleted the method that called `check_K8s_status`, `check_deployment_status`, `check_pods_restarted`, `ensure_pvc_exists` and `load_env_variables`, but kept the helpers themselves and kept the `check_K8s_status` import in `kubernetes_target`, so the name still greps as though something reaches it when nothing has for months. `check_K8s_status` is the one worth naming, because removing it retires a latent bug rather than only dead lines: it raised `None from Exception(...)` in two places, and `raise None` produces `TypeError: exceptions must derive from BaseException` instead of the "Kubernetes is not configured on this machine" message it meant to give. No user ever saw either, since the function was unreachable, and its live replacement is strictly better: `_load_cluster_config` performs the same kubeconfig-then-in-cluster fallback and raises a message naming `minikube start`, `KUBECONFIG` and `aws eks update-kubeconfig`. `_resolve_current_rs_hashes` and `_refresh_rs_hashes` read as part of the same cluster, being called only by `check_pods_restarted` inside this file, and deliberately stay because the unified target calls both directly. The tests that went covered only the removed functions and carried most of the nested mock patching in the scale suite, taking `test_k8s_utils.jac` from 534 lines to 119 while keeping its cluster-type, PVC resize and storage-quantity coverage. Pure deletion, no behavior change.
