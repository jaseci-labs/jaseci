- **Fix: jac-scale no longer silently destroys host logging config**: new `[scale.server] structured_logs = false` keeps handlers, formatters and levels untouched for embedding applications, and an explicitly-set root level now survives the default install instead of being forced to INFO.
