- **Fix: Scale K8s config cleanup**: Removed dead `[scale.kubernetes]` keys that parsed but drove nothing (`pvc_size`, `busybox_image`, `install_jaseci`, `jaseci_repo_url`/`jaseci_branch`/`jaseci_commit`, `plugin_versions`); corrected stale defaults and messages (per-service `hpa.cpu_target` documented default 70 -> 50, removed the never-read per-service `image_tag`, binary-prep error now points at `jaseci-labs/jac` releases, Grafana deploy log no longer claims `/grafana` ingress access unconditionally); labeled the Redis Service `managed: jac-scale` like every other resource; and microservice deployments now seed memory autoscaler triggers only when the service resolves a memory request, matching single-app behavior.
