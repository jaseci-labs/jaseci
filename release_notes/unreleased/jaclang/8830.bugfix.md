- **Fix: Long expressions no longer crash the compiler**: A statement holding thousands of chained operators, such as a long sum on one line, now compiles and runs instead of failing with an internal recursion error and no location. Programs also compile a little faster, since generating readable Python source is now deferred until a command actually asks for it.
