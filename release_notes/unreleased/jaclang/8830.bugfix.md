- **Fix: Long expressions no longer crash the compiler**: A statement holding thousands of chained operators, such as a long sum on one line, now compiles and runs instead of failing with an internal recursion error and no location. Past about six thousand operators the native compiler still gives up on the expression and falls back, which is reported as a warning; the program still builds and produces the right answer. Programs also compile a little faster, since generating readable Python source is now deferred until a command actually asks for it.
