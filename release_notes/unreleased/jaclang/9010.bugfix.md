- **Fix: generic variance checks for same-class assignments**: Generic arguments now honor covariant, contravariant, and invariant declarations when assigning between instances of the same generic class, so mutable containers such as `list[Dog]` are not accepted as `list[Animal]`.
