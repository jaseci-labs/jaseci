- **The jaclang.org site lives in-tree and ships in the binary (`jac create --awesome`)**: the whole production site (landing with a native-wasm arena game, live docs graph, ninja leaderboard, and littleX reborn as the Socialize section) now lives at `jac/examples/jaclang_org`, serves as CI's fullstack smoke via a real `jac browse` user journey (`scripts/site-browse-journey.sh`), and is bundled into the payload so `jac create <name> --awesome` scaffolds the entire thing as a runnable project. Landing this surfaced and fixed a batch of compiler defects: ES sourcemaps no longer emit negative positions for synthetic nodes (a multi-line f-string format spec could 500 the vite dev server), dev mode now emits native wasm modules for `/static/`, client wire-type collection follows import chains and falls back to parse-level resolution so `__from_wire` decoders never reference unemitted classes, and client `test` blocks get proper call classification (archetype `new`-emission and builtin lowering like `jid()`).
