- **Refactor: Share null-ok propagation in the lint pass**: The identical trailer null-ok propagation logic in `exit_if_else_expr` and `exit_bool_expr` is now a single `_propagate_null_ok_siblings` helper. No behavior change.
