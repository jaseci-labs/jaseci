- **Refactor: the JavaScript unparser is type-checked again**: The unparser carried a long-standing type-check exemption; clearing it corrected a local whose type was pinned too narrowly by its first assignment. A static JSX attribute's value is also declared as the literal it always holds, so a mismatched value is reported where it is built rather than quietly rewritten. Generated JavaScript is unchanged.
