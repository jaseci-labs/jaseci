- **Refactor: tighter declarations in the ecmascript backend**: A static JSX attribute's value is now declared as the literal it always holds, so a mismatched value is reported where it is built rather than quietly rewritten, and the view child union is written once instead of five times. No change to the generated JavaScript.
