- **Refactor: Reuse _make_name_node in the lint pass**: Three open-coded `uni.Name` constructions (in `_create_has_from_usestate` and `exit_expr_stmt`) now call the existing `_make_name_node` helper. No behavior change.
