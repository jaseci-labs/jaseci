- **Client: custom `<script>` tags in the generated `index.html` head**: `[client.app_meta_data]` now takes a `scripts` key for external (`src`) or inline (`content`) head scripts, with attribute names validated for well-formedness, applied in `jac build`, `jac start`, and `jac start --dev`.
