- **Fix: Host client build runs on the bundle's staged sources**: A scale deploy now builds the client bundle inside the same filtered staging copy that defines the `.jab`'s membership, so files the bundle strips (`.env`, the raw `jac.toml`, `.jacignore`-parked trees, key material) no longer reach the shipped dist. A build that references a file outside membership fails loud on the host, naming the file, the matching `.jacignore` pattern, and the fix.
