- **Refactor: a single root jac.toml carries manifest and config**: `jac/jac.toml` is deleted; the root `jac.toml` is now the one file holding the package manifest (`[project]` version/license/urls, `[test]` directory) and every config block, because config resolution walks up to the nearest ancestor `jac.toml` and one root file covers every cwd in the repo. Build and release readers repoint to it (mkpayload's version read, build.zig's payload input, the release/build-binaries workflow greps, release_utils' manifest resolution), and the `[check.lint]` exclude list is rebuilt on verified reasons: six entries for files that no longer exist are removed, `console.impl.jac`/`cli_boot.jac` are documented as no-print exemptions and deslopped once by hand, the generated dispatch/surface annexes are excluded by full path so the hand-written CLI `dispatch.impl.jac` is checked again, and `scripts/*` is codified so the precommit hook agrees with CI's fmt sweep.
