- **Fix: gateway /openapi.json aggregates concurrently**: one slow or dead service no longer adds its full timeout to the docs endpoint, and path/schema collisions are logged instead of silently resolved.
