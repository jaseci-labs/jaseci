- **Docs: the bundled docs and skills catch up to the current CLI, runtime, and compiler**: The corpus stops describing retired subsystems as current -- SQLite/Mongo/Redis and the FastAPI serving path become the Postgres store and the Jac-native serving stack, `jac start`/`jac serve`/`jac purge`/`jac js` spellings become the commands that exist, and the compiler internals describe the hand-written parser, the JCIR codegen tail, the unconditional analysis schedule, and the sealed native front end. `breaking-changes.md` gains the ten missing entries through #8621, the diagnostics reference gains E1122-E1125, and `jac check`'s own example string learns its flag is spelled `--parse_only`.
