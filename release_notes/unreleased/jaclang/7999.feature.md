- **byLLM: Stream tool-call arguments**: `by llm()` can now surface tool arguments while they are still being generated, so a caller can show which file is being written during a long write. Opt in with `[byllm.tool_stream] enabled = true`.
