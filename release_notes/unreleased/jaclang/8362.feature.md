- **Native brand binding has one driver-owned point**: `ensure_native_brands_bound` is the only function that reaches the materializer binder, and `JacCompiler`'s constructor calls it -- so a compiler cannot exist without class brands bound, no route can bind lazily from inside a predicate or entry, and every consumer records why it asked. Closes the E1053 binder-ordering bug class structurally.
