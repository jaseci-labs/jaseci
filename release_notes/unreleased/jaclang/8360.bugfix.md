- **Fix: the native verdict machinery stays in the aot lane, its resolver is bounded and context-deterministic, and a blast-radius guard makes the failure loud**: #8341's verdict-before-emission recorder treated the JIT lane's not-aot None as a pending verdict, so the link phase re-validated every import edge by compiling the running runtime's own sources with aot clones -- bytecode-tier callees carry no native IR, so every lookup missed by construction, and a cold installed-wheel context paid full price for each miss: the CI silent-hang avalanche. The recorder now fires only under aot_mode; the resolver refuses fail-closed (under the existing three-round E5028 bound) instead of compiling a callee that is inflight, mid-compile, or already being resolved; and its dep-cache arm falls through on an options-fingerprint mismatch instead of letting a stale cache file flip the verdict path a cold cache would take. A new blast-radius guard (E5029) refuses the link in seconds -- naming the taint count, the link-time compile count, and the fail-closed miss count -- so this class can never again present as a silent hour. Pinned in the demotion-link consistency suite, including a plain compile of the pack-smoke closure that must record zero taint and zero verdict misses.
