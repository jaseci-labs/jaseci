- **Fix: the native verdict machinery stays in the aot lane, and its link-time resolver is bounded**: #8341's verdict-before-emission recorder treated the JIT lane's not-aot None as a pending verdict, so in resolution contexts where layouts miss (installed wheel, dev-source off) the link phase re-validated every import edge by compiling the running runtime's own sources with aot clones -- a silent avalanche with hang phenomenology; the recorder now fires only under aot_mode. Its resolver also gains a floor: a callee that is inflight, mid-compile, or already being resolved is refused fail-closed (None, read as conflict-on-every-tainted-name) instead of handed back to prog.compile where the recursion had none, so a betrayed caller re-emits under the existing three-round E5028 bound rather than re-entering a schedule already on the stack. Both pinned in the demotion-link consistency suite; the sealed lane's record/re-emit behavior is unchanged.
