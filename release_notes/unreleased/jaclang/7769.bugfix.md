- **Fix: client dependency changes now reinstall, and missing runtime deps fail with a named error**: `install_packages` stamps `node_modules` with a fingerprint of the generated package.json and reinstalls whenever the dependency set changes, instead of skipping the install forever once `node_modules` exists; a new pre-build check verifies every npm package the compiled client runtime imports is actually installed and fails with the package name and remedy (`jac install` / `jac add --npm <pkg>`) instead of a truncated Rollup resolve trace, including a clear message when `JAC_CLIENT_SKIP_NPM_INSTALL` suppressed installation.
