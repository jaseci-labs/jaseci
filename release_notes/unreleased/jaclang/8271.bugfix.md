- **Fix: a merge artifact left two diagnostics sharing one code**: two different diagnostics were registered as E0109, silently shadowing one in reports and refusing the native link of the diagnostics module. The unsupported `\N{...}` escape refusal is E0110. Also clears the control-flow pass's checker escalations and its Python-package-init import as groundwork for a future sealing wave.
