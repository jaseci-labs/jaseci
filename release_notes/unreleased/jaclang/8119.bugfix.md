- **Fix: HTTP connections are reused instead of reopened**: Proxied requests, service-to-service calls and login flows now keep connections alive between requests, so they no longer pay a fresh connection setup every time.
