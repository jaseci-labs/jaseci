- **Refactor: Share glob/has combining in the lint pass**: The near-duplicate kid-rebuild in `combine_consecutive_glob` and `combine_consecutive_has` is now a single `_merge_run_into_base` helper parameterized by the item field and per-item semicolon handling. No behavior change.
