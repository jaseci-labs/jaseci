- **Sealing wave 2: three more compiler passes join the native seal**: The sem-def matcher (unblocked by native literal decoding), AST validation, and semantic analysis now ship as AOT native artifacts in sealed builds, each gated by the per-root seam coverage, enumerated waivers, and the reachability proof; the runtime tripwire and pass-serving binder cover them automatically. Also fixes a diagnostic-code collision from the literal-decoding work: the unsupported `\N{...}` escape refusal now has a code of its own instead of shadowing a sitting entry.
