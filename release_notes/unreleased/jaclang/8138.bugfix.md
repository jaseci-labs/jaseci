- **Fix: shadcn skill guide listed icon names that do not exist**: the `jac-shadcn-components` icon table gave `Calendar1Icon` and `Clock1Icon`, which `@hugeicons/core-free-icons` does not export, so agents copying the table produced an import that passes `jac check` and the build and then blanks the page at load. The table now gives `Calendar01Icon` and `Clock01Icon` and notes that numeric suffixes are always zero-padded.
