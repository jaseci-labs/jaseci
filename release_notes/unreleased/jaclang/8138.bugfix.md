- **Fix: shadcn skill guide no longer hands agents an icon name that does not exist**: the `jac-shadcn-components` icon table listed `Calendar1Icon` and `Clock1Icon` with the note "(single digit, NOT `01`)", but `@hugeicons/core-free-icons` exports neither under any name, so agents copying the table wrote an import that fails at page load -- a wrong named import is not a compile error, so `jac check` and the build both pass and the missing export blanks the page. The table now lists `Calendar01Icon` and `Clock01Icon` and states that numeric suffixes are always zero-padded. The nearby warning also claimed `SearchIcon`, `FolderIcon`, and `ArrowDownIcon` do not exist; all three are real aliases of their canonical `01` names, and only `DocumentIcon`, `PlusIcon`, and `TrendUpIcon` are genuinely absent.
