- **Compiler: `glob` in an impl-annex body now reports a clean error instead of an ICE**: A `glob` statement inside a function body defined in a separate `.impl.jac` annex was flagged `E0063` against the annex file, but the base module's codegen gate ignored annex-origin errors, so PyAST generation ran on the unlowered node and crashed with an ICE (and a "No bytecode found" downstream failure). Blocking errors originating in annex files now gate the base module's codegen, so the `E0063` diagnostic surfaces cleanly.
