- **Fix: checker preserves the callable signature under bare-`Callable` decorators**: decorating a function with a decorator typed as plain `Callable` erased the decorated function's parameter and return types from the symbol table, so a later call with wrong arguments passed checking. The checker now keeps the original signature when the decorator's own type carries no richer contract.
