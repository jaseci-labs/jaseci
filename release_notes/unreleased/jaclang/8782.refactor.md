- **Refactor: the dead code and stale config the graph-shaped compiler left behind**: A follow-up sweep over the #8744 clean break. Definitions nothing reaches are removed: `relink_light` (written for `detached_copy`, which rebuilds its edges in `UniNode.__deepcopy__` instead), the sync kernel's `run_typed` (only `run_untyped` is called; typed dispatch is inlined into the plan cache), `parse_source`/`parse_program`, `CodeLocInfo.update_first_token`/`update_last_token`, `InheritedSymbolTable`, the `AttachedComment` edge (the only relation edge with no users), and five uncalled entry points on the new stub catalog (`is_enum_scope`, `module_count`, `has_module`, `catalog_available`, `catalog_module_path`). Fifteen unused imports go with them. `jac.toml` no longer exempts `uni_pass.jac` and two deleted annexes from deslop on behalf of the retired `jac gen-uni-dispatch`, and `CONTRIBUTING.md`, `codegen-ir.md`, `.jacignore` and the lang-tools test docstring stop describing machinery that no longer exists. No behaviour change.
