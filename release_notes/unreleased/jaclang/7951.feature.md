- **Feature: lazy JIR dependency loading for `.pyi` stubs**: dependency modules now load through a single `JacProgram.load_dependency_module` seam that owns hub freshness, JIR cache read/write, source-compile fallback, and dependency recording; `.pyi` files are a first-class JIR cache kind with source-focused keys, and analysis imports route through the same path so warm stub caches skip recompilation in both the type checker and LSP. JIR format version bumps to 18 to invalidate pre-fix `arch_type` serialization in cached stubs.
