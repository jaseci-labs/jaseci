- Fixed `jac2js` lowering so bare JS host constructor calls in client code are automatically wrapped with `Reflect.construct`, preventing runtime `TypeError` from direct constructor invocation (#7651).
