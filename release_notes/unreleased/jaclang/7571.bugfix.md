- **Native: deep release is now O(1)-stack (#7446a)**: the transitive teardown inside `__rc_release_simple` is driven by an explicit heap worklist instead of C-stack recursion, so a long acyclic graph (linked list, deep AST) reclaims in constant stack space instead of SIGSEGVing on drop. Acyclic finalization order is unchanged; reference cycles still require `--gc cycles`. Contract documented in the `jac-native-memory` guide, with a stack-safety regression fixture.
