- **Fix: null-safe string comparison and enum-typed parameters in native-compiled code**: comparing a possibly-unset string (an `str | None` field left as `None`) against another string no longer crashes; a null operand now compares as if it were the empty string. Using an enum as a function parameter or return type also lowers correctly instead of failing to compile.
