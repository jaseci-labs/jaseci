- **Refactor: Cached atom-trailer collection in the lint pass**: `JacAutoLintPass._collect_atom_trailers` now reads the cached `get_all_sub_nodes` sub-node table instead of re-walking the subtree by hand on every call. No behavior change.
