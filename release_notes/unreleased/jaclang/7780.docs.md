- **Docs: marker retirement finished across the corpus**: the #7772 clean break de-marked every docs code block but left prose teaching the removed `sv`/`cl`/`na` markers, `sv import`, and the `.sv.jac`/`.cl.jac` suffixes, and its mechanical suffix replace garbled sentences (including the retired-marker diagnostic itself, which told users to "rename the file to .jac" from ".jac"). Every stale lesson now teaches inference and `[placement.pins]` over the already-migrated examples, the deleted variant-modules feature is gone from the reference, breaking-changes gains the missing #7772 entry with a migration table, and the diagnostic names `.cl.jac` again. All 852 docs code blocks still validate.
