- **Breaking: a named port is now a contract**: if `--port`, `--api_port` or `[serve] port` names a port that is already taken, `jac start` and `jac dev` now fail and name it instead of quietly relocating to the next free port; leaving the port unset is unchanged and still relocates with a notice.
