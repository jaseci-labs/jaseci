- **Breaking: a named port is now a contract**: if `--port`, `--api-port`, or a project kind's `[serve] port` names a port that is already taken, serving fails and names the port instead of quietly relocating to the next free one; leaving the port unset is unchanged and still relocates with a notice.
