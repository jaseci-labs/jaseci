A Python-only construct inside an `init` or `postinit` no longer costs the whole module its native lowering. A constructor is now a demotion owner like any other callable: it becomes a per-class native seam with a reason naming the blocking construct, its class layout and every sibling method still lower, and the seam is census-recorded, waiver-eligible, and shipped as an abort stub in a sealed artifact. Native construction of such a class refuses out loud with E5092 naming the demoted initializer rather than quietly skipping it, and the calling method cascades into a seam of its own. Constructs with no owning callable, such as a glob initializer or a field type, stay module-fatal.
