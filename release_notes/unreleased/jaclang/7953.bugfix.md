- **Fix: native power (`**`) lowered to invalid/unsound IR**: the integer power kernel now runs at the operand width (no more i32/i64 mix), finishes in the correct basic block with correct PHI predecessors, and honors Python/Jac semantics: `int ** negative` and a runtime-sign exponent yield `float`, and `0 ** negative` raises `ZeroDivisionError`. `**=` (locals, fields, lists, dicts) and builtin `pow(int, int)` route through the same lowering seam, and constant folding once again folds negative integer exponents.
