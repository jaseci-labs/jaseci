- **Fix: framework-aware client form dependencies and compiled-dir client detection**: the client form/validation dependencies are now keyed by framework, so Solid projects get `@tanstack/solid-form` injected instead of the react-only pair whose absence broke Solid client builds; `jac install` and `jac eject` now recognize a project as a client project when `.jac/client/compiled` exists, emitting the full runtime dependency closure for projects without a `[client]` table; and the default-deps prompt sources its version pins from the same constants as injection, removing the triplicated version literals that drifted.
