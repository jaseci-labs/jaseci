- **JacPython parser keeps trailing `**` kwargs after named keywords** (#8473): `pa_join_sequences` previously returned only the first gather sequence, so forms like `g(a=1, **d)` and `g(*a, b=1, **d)` dropped the double-starred mapping. It now concatenates gather sequences the same way CPython's `_PyPegen_join_sequences` does.
