- **Refactor: type errors in the compiler's own type checker**: Corrected several genuine type errors the checker flagged in `compiler/type_system` (`any`-typed members and untyped lists now carry their real `ClassMember`/`Symbol` types, argument-node parameters are widened to the `Sequence[Expr | KWPair] | None` callers actually pass, and optional values are guarded before being used where a concrete type is required). No change to user-facing behaviour.
