- **Fix: Destroying a scale app reclaims its namespace and storage**: `destroy` now removes the app's database, persistent volumes and the namespace it created, instead of leaving them running and billing, while a namespace it did not create is left in place.
