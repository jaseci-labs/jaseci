- **CI: typeshed stub cache invalidates again on payload-tool changes**: the `typeshed-stdlib` cache key hashed `jac/launcher/payload.zig`, a file deleted by the #8468 payload-tool rewrite, so edits under `jac/jaclang/dist/payload/**` no longer busted the stub cache and CI could bundle stale stdlib stubs. The key now hashes `jac/jaclang/dist/payload/**`. Stale documentation references to the removed `payload.zig` (CONTRIBUTING.md, typeshed PROVENANCE.md bump steps, `.gitignore` and `build.zig` comments) were updated to the current payload tool. Closes #7649.
