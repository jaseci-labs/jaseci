- **Refactor: the native seal leaves the compiler's native pathway**: The fused `libjac_compiler` seal (fused-root generation, waiver tables, the abort-stub scan, the four canaries, the pass tripwire and binder, and the native parser and prefix dispatch) is removed; the payload build precompiles the JIR image only and the manifest format moves to 8 without a `native` record. `jaclang/compiler/native_scope.jac` names the compiler modules served natively (empty for now) and a parity test pins the built image to it. `jac seal` and `jac check --compile-stats` are removed. User-program native compilation (`jac nacompile`, `jac build --native`) is unchanged. Tracking: #8732.
