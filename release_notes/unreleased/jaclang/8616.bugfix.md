- **Parser/pipeline: module-level docstring before an import no longer rejected**: a file starting with a docstring followed by an `import` was accepted by the jac0 parser but failed the full pipeline with `E0005: Unexpected token`. The grammar gap is fixed so the docstring-then-import form parses and compiles end to end. Closes #8541.
