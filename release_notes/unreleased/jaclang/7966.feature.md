- **Add: framework-neutral binary responses for `def:pub` HTTP functions**: Returning Jac's `BodyResponse` or `FileResponse` from a restspec function now skips the JSON transport envelope and serializer; use these values for binary bodies and file serving without importing FastAPI. Text/JSON callers that are not Jac clients continue to use `@restspec(envelope=False)`.
