- **Add: `Response` / `FileResponse` pass-through for `def:pub` HTTP functions**: Returning a Starlette/FastAPI `Response` (including `FileResponse`) from a restspec function now skips the JSON transport envelope and serializer; use this for binary downloads and file serving. Text/JSON callers that are not Jac clients continue to use `@restspec(envelope=False)`.
