- **Scale: the realization plan reports only what a realizer reads**: `RealizationPlan` advertised a gateway mode, a transport, a state backend and an injector flag that no realizer ever consulted, and the rollout log printed a state backend nothing was using. The plan now carries the environment and the topology, which is the field that actually gates local microservice serving.
