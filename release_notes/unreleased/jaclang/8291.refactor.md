- **The compact codegen IR becomes the only codegen, and the Python-AST emitter is deleted**: `JcirGenPass` and `JcirBytecodeGenPass` are the whole Python codegen tail. The emitter writes a 15-opcode container from the annotated unitree and the shim seat transcribes it back into `gen.py_ast`, `gen.py` and `gen.py_bytecode`, so every downstream consumer of the standard artifacts is unchanged. `jcir_gen_pass` joins `NATIVE_SEAL_ROOTS` with its closure and four waiver families, and the seal gains a container canary that refuses a producer whose bytes are not stable across two emissions or whose crossing loses a code-object field. The `JAC_CODEGEN` selector is gone rather than dormant, and with it `pyast_gen_pass`, `pybc_gen_pass`, `PyJacAstLinkPass`, the emitter's jsx processor and unitree's `DeleteStmt.py_ast_targets` accessor: 5,948 lines. The micro suite's per-node `jac_link` assertion becomes the loc-fill position check the emitter guarantees by construction, and the nineteen-suite payload canary manifest is pinned by a test instead of prose.
