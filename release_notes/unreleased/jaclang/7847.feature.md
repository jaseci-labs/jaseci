- **Lower-memory string handling in native code**: iterating over a string (`for c in s`) or indexing it (`s[i]`) in natively compiled code no longer allocates memory for each character, so string-heavy programs use far less memory and no longer leak while scanning text. Individual characters still behave exactly like normal strings (comparison, concatenation, length).
