- **Docs: implicit returns everywhere**: The fullstack tutorials, quick guide, plugin reference, and client-side skills now present JSX components in the implicit-return style introduced alongside the feature (`def:pub Badge(label: str) -> JsxElement { <span>{label}</span> }`), with early guard returns kept explicit. The language basics tutorial gains an Implicit Returns section covering the tail-expression rule.
