- **The evaluator's `**` gap was an annotation, and the joint seal's gate has one name**: the type-system coverage filed 30 of `type_evaluator.jac`'s 106 seams under `call argument unpacking '**'` -- `emit_diag`, its seven callers, and the 21 behind `get_type_of_expression` -- and the wave's plan was to rewrite the `emit_diag`-family call sites to explicit keywords or a typed options argument. Measured, no call site needed rewriting: the backend lowers `f(**kwargs)` whenever it can see a `**kwargs` sink on the callee, and `Transform.emit` forwards to `DiagnosticInfo.format_message(**kwargs)` with zero seams. What it cannot do is find a sink on a receiver typed `object`, and `TypeEvaluator._pass` was declared `object | None`, so the refusal named the argument shape at the call while the defect was the annotation on the field being called through. `_pass: Transform | None` takes the file to 80 seams across 41 causes with its closure unchanged, retiring the `'**'`, `emit_diag`, `'emit'` and `match_args_to_params` causes outright; `cur_node` and `errors_had` stay, because a method call through an erased slot lowers via the object bridge and a field read through one does not. Two dead imports (`PyastBuildPass`, `SymTabBuildPass`) and one hoisted into its single caller (`symbol_utils`) remove two of the three blockers that `native_default_path_ok` reported against the evaluator, leaving `PLACEMENT_INFERENCE_HELD["jac0core/codeinfo.jac"]` as the sole gate on the evaluator/checker joint seal -- the same entry that holds `transform.jac` out of every closure, and therefore the same entry behind `ownership_check_pass`'s eleven `emit` seams and `static_analysis_pass`'s twelfth. The one-closure flip is measured and recorded in the coverage rather than landed: declaring `has evaluator: TypeEvaluator` on the `class`-tier pass needs no constructor argument and retypes no body, but moves the boundary seams to `erased field` and adds two of its own until that entry is empty. Built in the configuration where it is empty, the joint seal measures a 28-module closure containing `type_evaluator.jac`, a checker coverage of 14 seams against 43 with 31 of the 36 boundary refusals clearing outright, and 3.40 GiB peak RSS -- inside the 4.5 GiB/root the seal pool already sizes on, so the grown closure needs no new capacity law.
