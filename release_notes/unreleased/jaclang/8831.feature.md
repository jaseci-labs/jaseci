- **Scale: the admin Runtime card shows the Jac version instead of the Python version**: the Prometheus collector registers a `jac_info` gauge with a `version` label, and the Metrics page reads it for the card.
