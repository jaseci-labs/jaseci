- **Fix: the CFG narrowing-key walk and `dataclasses.field` reach the native backend**: `CFGBuildPass` called `collect_narrowing_symbols` and `collect_assigned_symbols` from every branch visitor while both lived in the Python-side `symbol_utils`, so seven of the pass's eleven native seams were one live family; the bodies move to the native-clean `jaclang/compiler/expr_keys.jac` and `symbol_utils` re-exports them unchanged, taking the pass from eleven own seams to four. `dataclasses.field` is now a compile-time declaration-site marker like `typing.cast`: `field(default=V)` lowers to `V` and `field(default_factory=F)` lets the has-field default machinery build an empty container from the field's own type, which now covers sets as well as dicts and lists. That clears the module-scope refusal that left `jac0core/codeinfo.jac` producing no native IR at all, along with four other fatal constructs it was hiding.
