- **Refactor: Align lint property-role tables**: Removed the unreachable `cached_property` decorator-strip branch in `_convert_property_impl`; the role classifier never routes `cached_property` into conversion, so the two tables now agree. No behavior change.
