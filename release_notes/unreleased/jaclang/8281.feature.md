- **Sealing wave 5: the driver-fact readers join the native seal**: `jsx_intrinsic_guard_pass` and `access_check_pass` enter `NATIVE_SEAL_ROOTS`, bringing the sealed pass tier to thirteen native artifacts, both with zero own seams. Each used to answer a driver question itself: the jsx guard resolved the client kind from project config through `self.prog._compile_options`, and the access checker read `get_config` for its enforce flag and walked the filesystem for project roots on every cross-module name. Both facts are stamped on the module at the `run_schedule` seam now (`Module.mobui_enforced_root`, `Module.access_enforced`, `Module.jac_project_root`) and the passes read the slots. The wave-5 coverage test pins both closures, IR production, and the seam set two-way, and the sealed-seam reachability walk covers both roots' live entry surfaces.
