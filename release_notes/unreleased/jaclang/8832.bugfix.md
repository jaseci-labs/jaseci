- **Fix: Deeply nested expressions explain themselves**: When a program nests too deeply for the compiler to process, the error now says so and suggests splitting the expression, naming the file and line where it can. Previously it reported an internal recursion failure with no location.
