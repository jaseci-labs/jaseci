- **Strict port binding for `jac start`**: a port passed with `--port` is now a contract. If it is taken, the server fails instead of quietly moving to the next free one, which used to leave a proxy or port mapping pointing at nothing. Leaving `--port` unset keeps the old relocating behaviour.
