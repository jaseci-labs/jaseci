- **Docs: Scale scheduler guide**: Added a Scheduler & Background Jobs page to the Scale reference covering the `@schedule` decorator, static and dynamic schedules, the cron format, the `/jobs` REST API with curl examples, persistence, and the `[scale.scheduler]` configuration keys.
