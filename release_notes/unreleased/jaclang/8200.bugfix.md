The macOS native CI lane now runs the string and bytes primitive equivalence pins. They were filtered out, so darwin ran no string-primitive equivalence at all even though `str.lower()` lowers to a per-byte libc `tolower` call that the Linux and Mach-O link paths resolve to different implementations -- statically linked musl on Linux, `libSystem` on darwin.
