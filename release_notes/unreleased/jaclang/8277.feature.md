- **Sealing wave 4: the declaration binders join the native seal**: `sym_tab_build_pass` and `decl_impl_match_pass` enter `NATIVE_SEAL_ROOTS`, bringing the sealed pass tier to eleven native artifacts. sym_tab_build seals with one waived visitor seam (`exit_global_vars` calls the Python diagnostics machinery, the exact twin of the shipped `semantic_analysis` waiver) now that import detection is driver-stamped data; decl_impl_match seals with zero own seams. The wave-4 coverage test pins both closures, IR production, and the seam set two-way, and the sealed-seam reachability walk covers both roots' live entry surfaces.
