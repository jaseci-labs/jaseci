- **Scale: a successful deploy tells you how to reach the app**: `jac start --scale` ended at `Fleet ready` with no address, so a fleet deployed without an ingress was healthy and unreachable. The deploy result now carries the ingress URL when one was built, and otherwise the exact `kubectl port-forward` command for the gateway service in the namespace that was deployed, printed by both the CLI and the deploy SDK.
