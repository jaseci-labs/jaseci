- **Bare-container migration cluster 2 -- project, publish, and the seal driver say what they hold**: the `list`/`dict`/`set` annotations without element types across the project loader, the publish pathway, and the seal driver now name their real element types, continuing the #8323 decision-3 migration toward the repo-wide refusal.
