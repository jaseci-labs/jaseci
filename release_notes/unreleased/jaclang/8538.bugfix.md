- **The sealed-root seal drops a demotion waiver that no longer names a seam**: `compiler/type_system/type_utils.jac::completion_item_from_sym` lowers natively again, so its entry in `_TYPE_UTILS_SEAM_WAIVERS` waived nothing and the sealed-root demotion audit failed on every root that carries the closure waivers. The audit only runs on a source-lane runtime -- under a sealed jaclang image it skips, because a coverage there measures the image's trust boundary rather than each unit's own seams -- and CI's native lane runs sealed whenever the change touches native, so the staleness landed without a red build.
