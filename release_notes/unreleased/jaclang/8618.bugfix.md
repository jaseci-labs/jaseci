- **Fix: dev-source mode can no longer silently drop all semantic type diagnostics**: Adds `tests/compiler/test_typeshed_gate.jac`, a regression gate pinning the contract behind #8533 -- when a vendored typeshed stub is missing, compilation must fail loudly instead of silently accepting source the binary build rejects. Guards against reintroducing silent diagnostic loss in dev-source mode.
