- **Fix: `jac test` no longer grows memory with the number of test files**: A finished test file kept its module, and everything defined in it, alive for the rest of the run, so a long suite climbed steadily in memory and could exhaust a machine before finishing. Each file is now released as the runner moves on. On a 40-file measurement peak memory fell from 477 MB to 132 MB.
