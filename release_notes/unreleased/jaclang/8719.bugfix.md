- **Fix: the native backend's local slot map, default-triple target, and allocator reaches**: `local_vars` was declared `dict[str, ir.AllocaInstr]`, but it holds whatever `_entry_alloca` returns, which in the generator-resume case is a GEP into the frame rather than an alloca; the map now says `ir.Value`, matching what that function has always returned. `llvm.Target.from_default_triple` was annotated as returning `object` while doing nothing but delegating to `from_triple`, so `na_compile_pass` could not use its result even after asserting the type. And the bytes, clib ABI and object paths reached for `rc_alloc_fn` without first emitting the refcount helpers, so each call site saw an optional where an `ir.Function` is required. No runtime behaviour changes.
