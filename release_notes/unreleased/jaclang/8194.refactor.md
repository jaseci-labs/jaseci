- **Refactor: JIR tree caches eliminated**: Module JIR caches now persist bytecode only -- serialized AST trees are never written to or read from disk. Every process derives trees fresh from source, and a cache written by a different compiler build is rejected at validation instead of being served.
