- **Fix: using a possibly-`None` value in an operator is caught at check time**: `x + 1` where `x` is typed `int | None` used to pass `jac check` and then raise `TypeError` at runtime whenever `x` was `None`. The checker now reports it as `E1135`, mirroring how it already rejects attribute access on an un-narrowed optional. Arithmetic, bitwise, and shift operators are covered, in both plain (`x + 1`) and augmented (`x += 1`) form; guard the value with `if x is not None { ... }` (or an early return on the `None` case) first. Comparisons to `None` (`==`, `!=`, `is`) are unaffected.
