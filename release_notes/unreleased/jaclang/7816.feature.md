- **`jac build` honors the `[project] jac-version` pin**: the seal-producing projections (`--as jab`, `--as sealed`, `--as binary`) now require the running jac to satisfy the project's `jac-version` spec, erroring instead of sealing a bundle the pinned runtime cannot be trusted to load. Source distributions (`wheel`, `npm`) are not gated. Deploys already resolved the pin and seal with the pinned pod binary; build and deploy now agree.
