- **byLLM: the finish_tool narration call no longer breaks the Anthropic prompt cache**: `_stream_final_answer` shrank the tools array to `[finish_tool]` before streaming the final-answer narration after a successful `finish_tool` call; since Anthropic's cache breakpoint sits on `tools[-1]` and tools serialize ahead of the message list, that swap invalidated the cached prefix for the whole conversation on every turn that ended via `finish_tool`, rebilling the entire accumulated context at the cache-write rate. `tool_choice="none"` already forbade further tool calls on that turn, so the tools array is left untouched.
