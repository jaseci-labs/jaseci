- **Fix: short-circuit control flow for deeply nested boolean conditions**: a boolean condition with a compound boolean operand nested two or more levels deep (for example `a and (b and (c or d))`) built an incomplete control-flow graph, leaving a branch dangling. Such conditions now wire their short-circuit branches correctly.
