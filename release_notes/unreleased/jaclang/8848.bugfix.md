- **Fix: the admin HTTP traffic card reports latency, error rate and a real time window**: the card showed lifetime request counts under a "Last 60s" label, had no latency percentiles although the request-duration histogram was already in the payload, and pinned a "slowest path" banner on a lifetime mean. `/admin/metrics` now computes p50/p95/p99 by interpolating the histogram buckets, one row per endpoint with its error rate, and scopes traffic into app versus platform (admin portal, health probes, `/metrics`, `/docs`, `/openapi.json`); its `summary` carries `active_requests`, `scopes` and `endpoints` in place of the flat `total_requests`, `error_count` and `avg_latency_ms` fields, so the page only renders. The card shows a selectable 60s/5m/15m throughput chart with a separate error lane, labelled by how much of the window has actually been sampled, p50/p95/p99 and 4xx/5xx error-rate tiles, and an endpoint table sorted by p95. The garbage-collection note is derived from the counters instead of hardcoded.
