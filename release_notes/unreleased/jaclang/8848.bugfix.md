- **Fix: Admin HTTP traffic card reports latency, error rate and a real time window**: the card now reads the request-duration histogram buckets it was already being sent and shows p50/p95/p99, folds endpoint rows to one per method and path with an error-rate column, adds a selectable 60s/5m/15m window and an app/admin/health scope filter, and replaces the unlabelled bar row with an axed throughput chart plus a separate error lane. Also fixes a latency lookup that ignored the method label and overwrote instead of accumulating, counts 4xx as `client_error_count` in the metrics summary, and derives the garbage-collection note from the counters instead of hardcoding it.
