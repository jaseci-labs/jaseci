- **Fix: cold source builds no longer deadlock on the typeshed fetch**: The vendored typeshed stdlib is now materialized by a Zig bootstrap seed rather than by the Jac payload tool, which could not itself compile until the stubs it fetches were already on disk. `zig build fetch-typeshed` now works on a fresh checkout, and every build-tool invocation is ordered after both seed fetches.
