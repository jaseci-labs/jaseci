- **Feature: Erased distinct types (`type X := int`)**: a `:=` type alias brands its name nominally at check time while every lowering (bytecode, server, native) erases it to the base primitive; `X(v)` is a zero-cost cast. `obj X(builtin)` is now rejected (E1106) with a pointer at the distinct form. Also: the syntax arena owns its encoding behind a typed accessor surface, field-less archetypes keep inherited constructors, native pass crashes get a loud distinct coverage outcome, and a dual-codespace module glob that native code never references no longer aborts the native seal.
