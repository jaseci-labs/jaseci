- **Fix: Native-pathway and ownership-checker fixes from downstream dogfooding**: f-string text segments no longer lose quote characters when a segment between interpolations parses as a standalone literal (both backends); `os.path.isfile`/`isdir` read `st_mode` at the target's real offset so they work on darwin, `os.path.getsize` is implemented, and unsupported `os.path` members fail compilation with E5090 instead of emitting broken binaries; the ownership checker infers owned locals from str slices (which no longer consume their source), exempts typed-base enum members from E1401 and the compiler's own modules from ambient `[gc.enforce]` (fixing the rc_facts circular-import and inference-scheduling failures when a release binary drives a `[dev] jaclang_source` tree), and gives read-only builtin methods plus the native stdlib surface borrow contracts so `hay.find(pat)` and `os.system(cmd)` no longer consume or seal their operands; W6002 no longer flags Python-congruent `str.find`; W5032 treats ownership qualifiers on obj fields as layout-transparent; `JAC_NO_PRECOMPILE`/`JAC_REBUILD` are honored by the sealed precompiled-bundle lookup; docs and guide skills updated to match.
