# apps/android/ is fully regenerated by `jac build --client android`
# (gradle_scaffold + ComposeEmit.write). Only the Gradle wrapper is source.
apps/android/*
!apps/android/gradlew
!apps/android/gradlew.bat
!apps/android/gradle/
