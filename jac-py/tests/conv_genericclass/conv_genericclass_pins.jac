# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_genericclass.py

test "TestMROEntry.test_mro_entry_signature" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    tested = []\n\n    class B:\n        ...\n\n    class C:\n\n        def __mro_entries__(self, *args, **kwargs):\n            tested.extend([args, kwargs])\n            return (C,)\n    c = C()\n    assert tested == [], ('assertEqual', tested, [])\n\n    class D(B, c):\n        ...\n    assert tested[0] == ((B, c),), ('assertEqual', tested[0], ((B, c),))\n    assert tested[1] == {}, ('assertEqual', tested[1], {})\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_none" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    tested = []\n\n    class A:\n        ...\n\n    class B:\n        ...\n\n    class C:\n\n        def __mro_entries__(self, bases):\n            tested.append(bases)\n            return ()\n    c = C()\n    assert tested == [], ('assertEqual', tested, [])\n\n    class D(A, c, B):\n        ...\n    assert tested[-1] == (A, c, B), ('assertEqual', tested[-1], (A, c, B))\n    assert D.__bases__ == (A, B), ('assertEqual', D.__bases__, (A, B))\n    assert D.__orig_bases__ == (A, c, B), ('assertEqual', D.__orig_bases__, (A, c, B))\n    assert D.__mro__ == (D, A, B, object), ('assertEqual', D.__mro__, (D, A, B, object))\n\n    class E(c):\n        ...\n    assert tested[-1] == (c,), ('assertEqual', tested[-1], (c,))\n    assert E.__bases__ == (object,), ('assertEqual', E.__bases__, (object,))\n    assert E.__orig_bases__ == (c,), ('assertEqual', E.__orig_bases__, (c,))\n    assert E.__mro__ == (E, object), ('assertEqual', E.__mro__, (E, object))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_with_builtins" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    tested = []\n\n    class A:\n        ...\n\n    class C:\n\n        def __mro_entries__(self, bases):\n            tested.append(bases)\n            return (dict,)\n    c = C()\n    assert tested == [], ('assertEqual', tested, [])\n\n    class D(A, c):\n        ...\n    assert tested[-1] == (A, c), ('assertEqual', tested[-1], (A, c))\n    assert D.__bases__ == (A, dict), ('assertEqual', D.__bases__, (A, dict))\n    assert D.__orig_bases__ == (A, c), ('assertEqual', D.__orig_bases__, (A, c))\n    assert D.__mro__ == (D, A, dict, object), ('assertEqual', D.__mro__, (D, A, dict, object))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_with_builtins_2" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    tested = []\n\n    class C:\n\n        def __mro_entries__(self, bases):\n            tested.append(bases)\n            return (C,)\n    c = C()\n    assert tested == [], ('assertEqual', tested, [])\n\n    class D(c, dict):\n        ...\n    assert tested[-1] == (c, dict), ('assertEqual', tested[-1], (c, dict))\n    assert D.__bases__ == (C, dict), ('assertEqual', D.__bases__, (C, dict))\n    assert D.__orig_bases__ == (c, dict), ('assertEqual', D.__orig_bases__, (c, dict))\n    assert D.__mro__ == (D, C, dict, object), ('assertEqual', D.__mro__, (D, C, dict, object))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_errors" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C_too_many:\n\n        def __mro_entries__(self, bases, something, other):\n            return ()\n    c = C_too_many()\n    try:\n\n        class D(c):\n            ...\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class C_too_few:\n\n        def __mro_entries__(self):\n            return ()\n    d = C_too_few()\n    try:\n\n        class E(d):\n            ...\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_errors_2" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C_not_callable:\n        __mro_entries__ = 'Surprise!'\n    c = C_not_callable()\n    try:\n\n        class D(c):\n            ...\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class C_not_tuple:\n\n        def __mro_entries__(self):\n            return object\n    c = C_not_tuple()\n    try:\n\n        class E(c):\n            ...\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_metaclass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    meta_args = []\n\n    class Meta(type):\n\n        def __new__(mcls, name, bases, ns):\n            meta_args.extend([mcls, name, bases, ns])\n            return super().__new__(mcls, name, bases, ns)\n\n    class A:\n        ...\n\n    class C:\n\n        def __mro_entries__(self, bases):\n            return (A,)\n    c = C()\n\n    class D(c, metaclass=Meta):\n        x = 1\n    assert meta_args[0] == Meta, ('assertEqual', meta_args[0], Meta)\n    assert meta_args[1] == 'D', ('assertEqual', meta_args[1], 'D')\n    assert meta_args[2] == (A,), ('assertEqual', meta_args[2], (A,))\n    assert meta_args[3]['x'] == 1, ('assertEqual', meta_args[3]['x'], 1)\n    assert D.__bases__ == (A,), ('assertEqual', D.__bases__, (A,))\n    assert D.__orig_bases__ == (c,), ('assertEqual', D.__orig_bases__, (c,))\n    assert D.__mro__ == (D, A, object), ('assertEqual', D.__mro__, (D, A, object))\n    assert D.__class__ == Meta, ('assertEqual', D.__class__, Meta)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestMROEntry.test_mro_entry_type_call" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\n\ndef _t():\n\n    class C:\n\n        def __mro_entries__(self, bases):\n            return ()\n    c = C()\n    try:\n        type('Bad', (c,), {})\n    except TypeError as _exc:\n        assert _re.search('MRO entry resolution; use types.new_class()', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    getitem_args = []\n\n    class C:\n\n        def __class_getitem__(*args, **kwargs):\n            getitem_args.extend([args, kwargs])\n            return None\n    C[int, str]\n    assert getitem_args[0] == (C, (int, str)), ('assertEqual', getitem_args[0], (C, (int, str)))\n    assert getitem_args[1] == {}, ('assertEqual', getitem_args[1], {})\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_format" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        def __class_getitem__(cls, item):\n            return f'C[{item.__name__}]'\n    assert C[int] == 'C[int]', ('assertEqual', C[int], 'C[int]')\n    assert C[C] == 'C[C]', ('assertEqual', C[C], 'C[C]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_inheritance" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        def __class_getitem__(cls, item):\n            return f'{cls.__name__}[{item.__name__}]'\n\n    class D(C):\n        ...\n    assert D[int] == 'D[int]', ('assertEqual', D[int], 'D[int]')\n    assert D[D] == 'D[D]', ('assertEqual', D[D], 'D[D]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_inheritance_2" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        def __class_getitem__(cls, item):\n            return 'Should not see this'\n\n    class D(C):\n\n        def __class_getitem__(cls, item):\n            return f'{cls.__name__}[{item.__name__}]'\n    assert D[int] == 'D[int]', ('assertEqual', D[int], 'D[int]')\n    assert D[D] == 'D[D]', ('assertEqual', D[D], 'D[D]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_classmethod" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        @classmethod\n        def __class_getitem__(cls, item):\n            return f'{cls.__name__}[{item.__name__}]'\n\n    class D(C):\n        ...\n    assert D[int] == 'D[int]', ('assertEqual', D[int], 'D[int]')\n    assert D[D] == 'D[D]', ('assertEqual', D[D], 'D[D]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_patched" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        def __init_subclass__(cls):\n\n            def __class_getitem__(cls, item):\n                return f'{cls.__name__}[{item.__name__}]'\n            cls.__class_getitem__ = classmethod(__class_getitem__)\n\n    class D(C):\n        ...\n    assert D[int] == 'D[int]', ('assertEqual', D[int], 'D[int]')\n    assert D[D] == 'D[D]', ('assertEqual', D[D], 'D[D]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_with_builtins" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class A(dict):\n        called_with = None\n\n        def __class_getitem__(cls, item):\n            cls.called_with = item\n\n    class B(A):\n        pass\n    assert B.called_with is None, ('assertIs', B.called_with, None)\n    B[int]\n    assert B.called_with is int, ('assertIs', B.called_with, int)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_errors" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C_too_few:\n\n        def __class_getitem__(cls):\n            return None\n    try:\n        C_too_few[int]\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class C_too_many:\n\n        def __class_getitem__(cls, one, two):\n            return None\n    try:\n        C_too_many[int]\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_errors_2" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\n\ndef _t():\n\n    class C:\n\n        def __class_getitem__(cls, item):\n            return None\n    try:\n        C()[int]\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class E:\n        ...\n    e = E()\n    e.__class_getitem__ = lambda cls, item: 'This will not work'\n    try:\n        e[int]\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class C_not_callable:\n        __class_getitem__ = 'Surprise!'\n    try:\n        C_not_callable[int]\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\n    class C_is_none(tuple):\n        __class_getitem__ = None\n    try:\n        C_is_none[int]\n    except TypeError as _exc:\n        assert _re.search('C_is_none', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_metaclass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class Meta(type):\n\n        def __class_getitem__(cls, item):\n            return f'{cls.__name__}[{item.__name__}]'\n    assert Meta[int] == 'Meta[int]', ('assertEqual', Meta[int], 'Meta[int]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_with_metaclass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class Meta(type):\n        pass\n\n    class C(metaclass=Meta):\n\n        def __class_getitem__(cls, item):\n            return f'{cls.__name__}[{item.__name__}]'\n    assert C[int] == 'C[int]', ('assertEqual', C[int], 'C[int]')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestClassGetitem.test_class_getitem_metaclass_first" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class Meta(type):\n\n        def __getitem__(cls, item):\n            return 'from metaclass'\n\n    class C(metaclass=Meta):\n\n        def __class_getitem__(cls, item):\n            return 'from __class_getitem__'\n    assert C[int] == 'from metaclass', ('assertEqual', C[int], 'from metaclass')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
