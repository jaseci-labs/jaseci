{
  "schema_version": 1,
  "tool_version": "conv_suite-0.4.0",
  "jac_sha": "c01c89da056bb6d42e60df316b7e375e687a90dd",
  "cpython": {
    "version": "3.14.6",
    "sha": "c63aec69bd59c55314c06c23f4c22c03de76fe45"
  },
  "command": [
    "convert_suite.py",
    "reference/cpython/Lib/test/test_compare.py",
    "-o",
    "/var/tmp/sp6-out/test_compare",
    "--name",
    "conv_compare"
  ],
  "generated_at": "2026-08-24T13:51:37",
  "hashes": {
    "pins_jac": "f529ab3dd88a496e68f66735216edd7b694fe288cb261b048383e21e1b30783f"
  },
  "source_file": "reference/cpython/Lib/test/test_compare.py",
  "generator": "jac-py/tools/convert_suite.py",
  "module_stem": "compare",
  "pins_file": "conv_compare_pins.jac",
  "pins": [
    {
      "ident": "ComparisonSimpleTest.test_issue_1393",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import ALWAYS_EQ\n\ndef _t():\n    x = lambda: None\n    assert x == ALWAYS_EQ, ('assertEqual', x, ALWAYS_EQ)\n    assert ALWAYS_EQ == x, ('assertEqual', ALWAYS_EQ, x)\n    y = object()\n    assert y == ALWAYS_EQ, ('assertEqual', y, ALWAYS_EQ)\n    assert ALWAYS_EQ == y, ('assertEqual', ALWAYS_EQ, y)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_objects",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_equality_only(a, b, equal):\n    \"\"\"Assert equality result and that ordering is not implemented.\n\n        a, b: Instances to be tested (of same or different type).\n        equal: Boolean indicating the expected equality comparison results.\n        \"\"\"\n    assert (a == b) == equal, ('assertEqual', (a == b), equal)\n    assert (b == a) == equal, ('assertEqual', (b == a), equal)\n    assert (a != b) == (not equal), ('assertEqual', (a != b), (not equal))\n    assert (b != a) == (not equal), ('assertEqual', (b != a), (not equal))\n    try:\n        a < b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a <= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a > b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a >= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b < a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b <= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b > a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b >= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\ndef _t():\n    \"\"\"Compare instances of type 'object'.\"\"\"\n    a = object()\n    b = object()\n    assert_equality_only(a, a, True)\n    assert_equality_only(a, b, False)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_str_subclass",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_eq_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'eq' in a_meth or 'eq' in b_meth:\n        assert (a == b) == (comp == 0), ('assertEqual', (a == b), (comp == 0))\n        assert (b == a) == (comp == 0), ('assertEqual', (b == a), (comp == 0))\n    else:\n        assert (a == b) == (a is b), ('assertEqual', (a == b), (a is b))\n        assert (b == a) == (a is b), ('assertEqual', (b == a), (a is b))\n\ndef assert_ne_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or not {'ne', 'eq'}.isdisjoint(a_meth + b_meth):\n        assert (a != b) == (comp != 0), ('assertEqual', (a != b), (comp != 0))\n        assert (b != a) == (comp != 0), ('assertEqual', (b != a), (comp != 0))\n    else:\n        assert (a != b) == (a is not b), ('assertEqual', (a != b), (a is not b))\n        assert (b != a) == (a is not b), ('assertEqual', (b != a), (a is not b))\n\ndef assert_lt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'lt' in a_meth or 'gt' in b_meth:\n        assert (a < b) == (comp < 0), ('assertEqual', (a < b), (comp < 0))\n        assert (b > a) == (comp < 0), ('assertEqual', (b > a), (comp < 0))\n    else:\n        try:\n            a < b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b > a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_le_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'le' in a_meth or 'ge' in b_meth:\n        assert (a <= b) == (comp <= 0), ('assertEqual', (a <= b), (comp <= 0))\n        assert (b >= a) == (comp <= 0), ('assertEqual', (b >= a), (comp <= 0))\n    else:\n        try:\n            a <= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b >= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_gt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'gt' in a_meth or 'lt' in b_meth:\n        assert (a > b) == (comp > 0), ('assertEqual', (a > b), (comp > 0))\n        assert (b < a) == (comp > 0), ('assertEqual', (b < a), (comp > 0))\n    else:\n        try:\n            a > b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b < a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_ge_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'ge' in a_meth or 'le' in b_meth:\n        assert (a >= b) == (comp >= 0), ('assertEqual', (a >= b), (comp >= 0))\n        assert (b <= a) == (comp >= 0), ('assertEqual', (b <= a), (comp >= 0))\n    else:\n        try:\n            a >= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b <= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_total_order(a, b, comp, a_meth=None, b_meth=None):\n    \"\"\"Test total ordering comparison of two instances.\n\n        a, b: Instances to be tested (of same or different type).\n\n        comp: -1, 0, or 1 indicates that the expected order comparison\n           result for operations that are supported by the classes is\n           a <, ==, or > b.\n\n        a_meth, b_meth: Either None, indicating that all rich comparison\n           methods are available, aa for builtins, or the tuple (subset)\n           of \"eq\", \"ne\", \"lt\", \"le\", \"gt\", and \"ge\" that are available\n           for the corresponding instance (of a user-defined class).\n        \"\"\"\n    assert_eq_subtest(a, b, comp, a_meth, b_meth)\n    assert_ne_subtest(a, b, comp, a_meth, b_meth)\n    assert_lt_subtest(a, b, comp, a_meth, b_meth)\n    assert_le_subtest(a, b, comp, a_meth, b_meth)\n    assert_gt_subtest(a, b, comp, a_meth, b_meth)\n    assert_ge_subtest(a, b, comp, a_meth, b_meth)\n\ndef _t():\n    \"\"\"Compare instances of str and a subclass.\"\"\"\n\n    class StrSubclass(str):\n        pass\n    s1 = str('a')\n    s2 = str('b')\n    c1 = StrSubclass('a')\n    c2 = StrSubclass('b')\n    c3 = StrSubclass('b')\n    assert_total_order(s1, s1, 0)\n    assert_total_order(s1, s2, -1)\n    assert_total_order(c1, c1, 0)\n    assert_total_order(c1, c2, -1)\n    assert_total_order(c2, c3, 0)\n    assert_total_order(s1, c2, -1)\n    assert_total_order(s2, c3, 0)\n    assert_total_order(c1, s2, -1)\n    assert_total_order(c2, s2, 0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_numbers",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\nfrom fractions import Fraction\nfrom decimal import Decimal\n\ndef assert_eq_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'eq' in a_meth or 'eq' in b_meth:\n        assert (a == b) == (comp == 0), ('assertEqual', (a == b), (comp == 0))\n        assert (b == a) == (comp == 0), ('assertEqual', (b == a), (comp == 0))\n    else:\n        assert (a == b) == (a is b), ('assertEqual', (a == b), (a is b))\n        assert (b == a) == (a is b), ('assertEqual', (b == a), (a is b))\n\ndef assert_ne_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or not {'ne', 'eq'}.isdisjoint(a_meth + b_meth):\n        assert (a != b) == (comp != 0), ('assertEqual', (a != b), (comp != 0))\n        assert (b != a) == (comp != 0), ('assertEqual', (b != a), (comp != 0))\n    else:\n        assert (a != b) == (a is not b), ('assertEqual', (a != b), (a is not b))\n        assert (b != a) == (a is not b), ('assertEqual', (b != a), (a is not b))\n\ndef assert_lt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'lt' in a_meth or 'gt' in b_meth:\n        assert (a < b) == (comp < 0), ('assertEqual', (a < b), (comp < 0))\n        assert (b > a) == (comp < 0), ('assertEqual', (b > a), (comp < 0))\n    else:\n        try:\n            a < b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b > a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_le_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'le' in a_meth or 'ge' in b_meth:\n        assert (a <= b) == (comp <= 0), ('assertEqual', (a <= b), (comp <= 0))\n        assert (b >= a) == (comp <= 0), ('assertEqual', (b >= a), (comp <= 0))\n    else:\n        try:\n            a <= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b >= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_gt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'gt' in a_meth or 'lt' in b_meth:\n        assert (a > b) == (comp > 0), ('assertEqual', (a > b), (comp > 0))\n        assert (b < a) == (comp > 0), ('assertEqual', (b < a), (comp > 0))\n    else:\n        try:\n            a > b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b < a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_ge_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'ge' in a_meth or 'le' in b_meth:\n        assert (a >= b) == (comp >= 0), ('assertEqual', (a >= b), (comp >= 0))\n        assert (b <= a) == (comp >= 0), ('assertEqual', (b <= a), (comp >= 0))\n    else:\n        try:\n            a >= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b <= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_total_order(a, b, comp, a_meth=None, b_meth=None):\n    \"\"\"Test total ordering comparison of two instances.\n\n        a, b: Instances to be tested (of same or different type).\n\n        comp: -1, 0, or 1 indicates that the expected order comparison\n           result for operations that are supported by the classes is\n           a <, ==, or > b.\n\n        a_meth, b_meth: Either None, indicating that all rich comparison\n           methods are available, aa for builtins, or the tuple (subset)\n           of \"eq\", \"ne\", \"lt\", \"le\", \"gt\", and \"ge\" that are available\n           for the corresponding instance (of a user-defined class).\n        \"\"\"\n    assert_eq_subtest(a, b, comp, a_meth, b_meth)\n    assert_ne_subtest(a, b, comp, a_meth, b_meth)\n    assert_lt_subtest(a, b, comp, a_meth, b_meth)\n    assert_le_subtest(a, b, comp, a_meth, b_meth)\n    assert_gt_subtest(a, b, comp, a_meth, b_meth)\n    assert_ge_subtest(a, b, comp, a_meth, b_meth)\n\ndef assert_equality_only(a, b, equal):\n    \"\"\"Assert equality result and that ordering is not implemented.\n\n        a, b: Instances to be tested (of same or different type).\n        equal: Boolean indicating the expected equality comparison results.\n        \"\"\"\n    assert (a == b) == equal, ('assertEqual', (a == b), equal)\n    assert (b == a) == equal, ('assertEqual', (b == a), equal)\n    assert (a != b) == (not equal), ('assertEqual', (a != b), (not equal))\n    assert (b != a) == (not equal), ('assertEqual', (b != a), (not equal))\n    try:\n        a < b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a <= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a > b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a >= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b < a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b <= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b > a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b >= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\ndef _t():\n    \"\"\"Compare number types.\"\"\"\n    i1 = 1001\n    i2 = 1002\n    assert_total_order(i1, i1, 0)\n    assert_total_order(i1, i2, -1)\n    f1 = 1001.0\n    f2 = 1001.1\n    assert_total_order(f1, f1, 0)\n    assert_total_order(f1, f2, -1)\n    q1 = Fraction(2002, 2)\n    q2 = Fraction(2003, 2)\n    assert_total_order(q1, q1, 0)\n    assert_total_order(q1, q2, -1)\n    d1 = Decimal('1001.0')\n    d2 = Decimal('1001.1')\n    assert_total_order(d1, d1, 0)\n    assert_total_order(d1, d2, -1)\n    c1 = 1001 + 0j\n    c2 = 1001 + 1j\n    assert_equality_only(c1, c1, True)\n    assert_equality_only(c1, c2, False)\n    for n1, n2 in ((i1, f1), (i1, q1), (i1, d1), (f1, q1), (f1, d1), (q1, d1)):\n        assert_total_order(n1, n2, 0)\n    for n1 in (i1, f1, q1, d1):\n        assert_equality_only(n1, c1, True)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_sequences",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_eq_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'eq' in a_meth or 'eq' in b_meth:\n        assert (a == b) == (comp == 0), ('assertEqual', (a == b), (comp == 0))\n        assert (b == a) == (comp == 0), ('assertEqual', (b == a), (comp == 0))\n    else:\n        assert (a == b) == (a is b), ('assertEqual', (a == b), (a is b))\n        assert (b == a) == (a is b), ('assertEqual', (b == a), (a is b))\n\ndef assert_ne_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or not {'ne', 'eq'}.isdisjoint(a_meth + b_meth):\n        assert (a != b) == (comp != 0), ('assertEqual', (a != b), (comp != 0))\n        assert (b != a) == (comp != 0), ('assertEqual', (b != a), (comp != 0))\n    else:\n        assert (a != b) == (a is not b), ('assertEqual', (a != b), (a is not b))\n        assert (b != a) == (a is not b), ('assertEqual', (b != a), (a is not b))\n\ndef assert_lt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'lt' in a_meth or 'gt' in b_meth:\n        assert (a < b) == (comp < 0), ('assertEqual', (a < b), (comp < 0))\n        assert (b > a) == (comp < 0), ('assertEqual', (b > a), (comp < 0))\n    else:\n        try:\n            a < b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b > a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_le_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'le' in a_meth or 'ge' in b_meth:\n        assert (a <= b) == (comp <= 0), ('assertEqual', (a <= b), (comp <= 0))\n        assert (b >= a) == (comp <= 0), ('assertEqual', (b >= a), (comp <= 0))\n    else:\n        try:\n            a <= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b >= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_gt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'gt' in a_meth or 'lt' in b_meth:\n        assert (a > b) == (comp > 0), ('assertEqual', (a > b), (comp > 0))\n        assert (b < a) == (comp > 0), ('assertEqual', (b < a), (comp > 0))\n    else:\n        try:\n            a > b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b < a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_ge_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'ge' in a_meth or 'le' in b_meth:\n        assert (a >= b) == (comp >= 0), ('assertEqual', (a >= b), (comp >= 0))\n        assert (b <= a) == (comp >= 0), ('assertEqual', (b <= a), (comp >= 0))\n    else:\n        try:\n            a >= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b <= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_total_order(a, b, comp, a_meth=None, b_meth=None):\n    \"\"\"Test total ordering comparison of two instances.\n\n        a, b: Instances to be tested (of same or different type).\n\n        comp: -1, 0, or 1 indicates that the expected order comparison\n           result for operations that are supported by the classes is\n           a <, ==, or > b.\n\n        a_meth, b_meth: Either None, indicating that all rich comparison\n           methods are available, aa for builtins, or the tuple (subset)\n           of \"eq\", \"ne\", \"lt\", \"le\", \"gt\", and \"ge\" that are available\n           for the corresponding instance (of a user-defined class).\n        \"\"\"\n    assert_eq_subtest(a, b, comp, a_meth, b_meth)\n    assert_ne_subtest(a, b, comp, a_meth, b_meth)\n    assert_lt_subtest(a, b, comp, a_meth, b_meth)\n    assert_le_subtest(a, b, comp, a_meth, b_meth)\n    assert_gt_subtest(a, b, comp, a_meth, b_meth)\n    assert_ge_subtest(a, b, comp, a_meth, b_meth)\n\ndef assert_equality_only(a, b, equal):\n    \"\"\"Assert equality result and that ordering is not implemented.\n\n        a, b: Instances to be tested (of same or different type).\n        equal: Boolean indicating the expected equality comparison results.\n        \"\"\"\n    assert (a == b) == equal, ('assertEqual', (a == b), equal)\n    assert (b == a) == equal, ('assertEqual', (b == a), equal)\n    assert (a != b) == (not equal), ('assertEqual', (a != b), (not equal))\n    assert (b != a) == (not equal), ('assertEqual', (b != a), (not equal))\n    try:\n        a < b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a <= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a > b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a >= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b < a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b <= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b > a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b >= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\ndef _t():\n    \"\"\"Compare list, tuple, and range.\"\"\"\n    l1 = [1, 2]\n    l2 = [2, 3]\n    assert_total_order(l1, l1, 0)\n    assert_total_order(l1, l2, -1)\n    t1 = (1, 2)\n    t2 = (2, 3)\n    assert_total_order(t1, t1, 0)\n    assert_total_order(t1, t2, -1)\n    r1 = range(1, 2)\n    r2 = range(2, 2)\n    assert_equality_only(r1, r1, True)\n    assert_equality_only(r1, r2, False)\n    assert_equality_only(t1, l1, False)\n    assert_equality_only(l1, r1, False)\n    assert_equality_only(r1, t1, False)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_bytes",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_eq_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'eq' in a_meth or 'eq' in b_meth:\n        assert (a == b) == (comp == 0), ('assertEqual', (a == b), (comp == 0))\n        assert (b == a) == (comp == 0), ('assertEqual', (b == a), (comp == 0))\n    else:\n        assert (a == b) == (a is b), ('assertEqual', (a == b), (a is b))\n        assert (b == a) == (a is b), ('assertEqual', (b == a), (a is b))\n\ndef assert_ne_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or not {'ne', 'eq'}.isdisjoint(a_meth + b_meth):\n        assert (a != b) == (comp != 0), ('assertEqual', (a != b), (comp != 0))\n        assert (b != a) == (comp != 0), ('assertEqual', (b != a), (comp != 0))\n    else:\n        assert (a != b) == (a is not b), ('assertEqual', (a != b), (a is not b))\n        assert (b != a) == (a is not b), ('assertEqual', (b != a), (a is not b))\n\ndef assert_lt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'lt' in a_meth or 'gt' in b_meth:\n        assert (a < b) == (comp < 0), ('assertEqual', (a < b), (comp < 0))\n        assert (b > a) == (comp < 0), ('assertEqual', (b > a), (comp < 0))\n    else:\n        try:\n            a < b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b > a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_le_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'le' in a_meth or 'ge' in b_meth:\n        assert (a <= b) == (comp <= 0), ('assertEqual', (a <= b), (comp <= 0))\n        assert (b >= a) == (comp <= 0), ('assertEqual', (b >= a), (comp <= 0))\n    else:\n        try:\n            a <= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b >= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_gt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'gt' in a_meth or 'lt' in b_meth:\n        assert (a > b) == (comp > 0), ('assertEqual', (a > b), (comp > 0))\n        assert (b < a) == (comp > 0), ('assertEqual', (b < a), (comp > 0))\n    else:\n        try:\n            a > b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b < a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_ge_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'ge' in a_meth or 'le' in b_meth:\n        assert (a >= b) == (comp >= 0), ('assertEqual', (a >= b), (comp >= 0))\n        assert (b <= a) == (comp >= 0), ('assertEqual', (b <= a), (comp >= 0))\n    else:\n        try:\n            a >= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b <= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_total_order(a, b, comp, a_meth=None, b_meth=None):\n    \"\"\"Test total ordering comparison of two instances.\n\n        a, b: Instances to be tested (of same or different type).\n\n        comp: -1, 0, or 1 indicates that the expected order comparison\n           result for operations that are supported by the classes is\n           a <, ==, or > b.\n\n        a_meth, b_meth: Either None, indicating that all rich comparison\n           methods are available, aa for builtins, or the tuple (subset)\n           of \"eq\", \"ne\", \"lt\", \"le\", \"gt\", and \"ge\" that are available\n           for the corresponding instance (of a user-defined class).\n        \"\"\"\n    assert_eq_subtest(a, b, comp, a_meth, b_meth)\n    assert_ne_subtest(a, b, comp, a_meth, b_meth)\n    assert_lt_subtest(a, b, comp, a_meth, b_meth)\n    assert_le_subtest(a, b, comp, a_meth, b_meth)\n    assert_gt_subtest(a, b, comp, a_meth, b_meth)\n    assert_ge_subtest(a, b, comp, a_meth, b_meth)\n\ndef _t():\n    \"\"\"Compare bytes and bytearray.\"\"\"\n    bs1 = b'a1'\n    bs2 = b'b2'\n    assert_total_order(bs1, bs1, 0)\n    assert_total_order(bs1, bs2, -1)\n    ba1 = bytearray(b'a1')\n    ba2 = bytearray(b'b2')\n    assert_total_order(ba1, ba1, 0)\n    assert_total_order(ba1, ba2, -1)\n    assert_total_order(bs1, ba1, 0)\n    assert_total_order(bs1, ba2, -1)\n    assert_total_order(ba1, bs1, 0)\n    assert_total_order(ba1, bs2, -1)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_sets",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_eq_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'eq' in a_meth or 'eq' in b_meth:\n        assert (a == b) == (comp == 0), ('assertEqual', (a == b), (comp == 0))\n        assert (b == a) == (comp == 0), ('assertEqual', (b == a), (comp == 0))\n    else:\n        assert (a == b) == (a is b), ('assertEqual', (a == b), (a is b))\n        assert (b == a) == (a is b), ('assertEqual', (b == a), (a is b))\n\ndef assert_ne_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or not {'ne', 'eq'}.isdisjoint(a_meth + b_meth):\n        assert (a != b) == (comp != 0), ('assertEqual', (a != b), (comp != 0))\n        assert (b != a) == (comp != 0), ('assertEqual', (b != a), (comp != 0))\n    else:\n        assert (a != b) == (a is not b), ('assertEqual', (a != b), (a is not b))\n        assert (b != a) == (a is not b), ('assertEqual', (b != a), (a is not b))\n\ndef assert_lt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'lt' in a_meth or 'gt' in b_meth:\n        assert (a < b) == (comp < 0), ('assertEqual', (a < b), (comp < 0))\n        assert (b > a) == (comp < 0), ('assertEqual', (b > a), (comp < 0))\n    else:\n        try:\n            a < b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b > a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_le_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'le' in a_meth or 'ge' in b_meth:\n        assert (a <= b) == (comp <= 0), ('assertEqual', (a <= b), (comp <= 0))\n        assert (b >= a) == (comp <= 0), ('assertEqual', (b >= a), (comp <= 0))\n    else:\n        try:\n            a <= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b >= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_gt_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'gt' in a_meth or 'lt' in b_meth:\n        assert (a > b) == (comp > 0), ('assertEqual', (a > b), (comp > 0))\n        assert (b < a) == (comp > 0), ('assertEqual', (b < a), (comp > 0))\n    else:\n        try:\n            a > b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b < a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_ge_subtest(a, b, comp, a_meth, b_meth):\n    if a_meth is None or 'ge' in a_meth or 'le' in b_meth:\n        assert (a >= b) == (comp >= 0), ('assertEqual', (a >= b), (comp >= 0))\n        assert (b <= a) == (comp >= 0), ('assertEqual', (b <= a), (comp >= 0))\n    else:\n        try:\n            a >= b\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n        try:\n            b <= a\n        except TypeError as _exc:\n            assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n        else:\n            raise AssertionError('assertRaises: did not raise')\n\ndef assert_total_order(a, b, comp, a_meth=None, b_meth=None):\n    \"\"\"Test total ordering comparison of two instances.\n\n        a, b: Instances to be tested (of same or different type).\n\n        comp: -1, 0, or 1 indicates that the expected order comparison\n           result for operations that are supported by the classes is\n           a <, ==, or > b.\n\n        a_meth, b_meth: Either None, indicating that all rich comparison\n           methods are available, aa for builtins, or the tuple (subset)\n           of \"eq\", \"ne\", \"lt\", \"le\", \"gt\", and \"ge\" that are available\n           for the corresponding instance (of a user-defined class).\n        \"\"\"\n    assert_eq_subtest(a, b, comp, a_meth, b_meth)\n    assert_ne_subtest(a, b, comp, a_meth, b_meth)\n    assert_lt_subtest(a, b, comp, a_meth, b_meth)\n    assert_le_subtest(a, b, comp, a_meth, b_meth)\n    assert_gt_subtest(a, b, comp, a_meth, b_meth)\n    assert_ge_subtest(a, b, comp, a_meth, b_meth)\n\ndef _t():\n    \"\"\"Compare set and frozenset.\"\"\"\n    s1 = {1, 2}\n    s2 = {1, 2, 3}\n    assert_total_order(s1, s1, 0)\n    assert_total_order(s1, s2, -1)\n    f1 = frozenset(s1)\n    f2 = frozenset(s2)\n    assert_total_order(f1, f1, 0)\n    assert_total_order(f1, f2, -1)\n    assert_total_order(s1, f1, 0)\n    assert_total_order(s1, f2, -1)\n    assert_total_order(f1, s1, 0)\n    assert_total_order(f1, s2, -1)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "ComparisonFullTest.test_mappings",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import re as _re\n\ndef assert_equality_only(a, b, equal):\n    \"\"\"Assert equality result and that ordering is not implemented.\n\n        a, b: Instances to be tested (of same or different type).\n        equal: Boolean indicating the expected equality comparison results.\n        \"\"\"\n    assert (a == b) == equal, ('assertEqual', (a == b), equal)\n    assert (b == a) == equal, ('assertEqual', (b == a), equal)\n    assert (a != b) == (not equal), ('assertEqual', (a != b), (not equal))\n    assert (b != a) == (not equal), ('assertEqual', (b != a), (not equal))\n    try:\n        a < b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a <= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a > b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        a >= b\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b < a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b <= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b > a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        b >= a\n    except TypeError as _exc:\n        assert _re.search('not supported', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n\ndef _t():\n    \"\"\" Compare dict.\n        \"\"\"\n    d1 = {1: 'a', 2: 'b'}\n    d2 = {2: 'b', 3: 'c'}\n    d3 = {3: 'c', 2: 'b'}\n    assert_equality_only(d1, d1, True)\n    assert_equality_only(d1, d2, False)\n    assert_equality_only(d2, d3, True)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    }
  ],
  "quarantined": [
    {
      "ident": "ComparisonSimpleTest.test_comparisons",
      "reason": "uses-self.candidates"
    },
    {
      "ident": "ComparisonSimpleTest.test_id_comparisons",
      "reason": "uses-self.Empty"
    },
    {
      "ident": "ComparisonSimpleTest.test_ne_defaults_to_not_eq",
      "reason": "uses-self.Cmp"
    },
    {
      "ident": "ComparisonSimpleTest.test_ne_high_priority",
      "reason": "self.assertSequenceEqual"
    },
    {
      "ident": "ComparisonSimpleTest.test_ne_low_priority",
      "reason": "self.assertSequenceEqual"
    },
    {
      "ident": "ComparisonSimpleTest.test_other_delegation",
      "reason": "uses-self.subTest"
    },
    {
      "ident": "ComparisonFullTest.test_comp_classes_same",
      "reason": "uses-self.all_comp_classes"
    },
    {
      "ident": "ComparisonFullTest.test_comp_classes_different",
      "reason": "uses-self.all_comp_classes"
    }
  ],
  "counts": {
    "extracted": 16,
    "pinned": 8,
    "quarantined": 8
  }
}
