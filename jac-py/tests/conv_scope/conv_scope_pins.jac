# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_scope.py

test "ScopeTests.testSimpleNesting" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder(x):\n\n        def adder(y):\n            return x + y\n        return adder\n    inc = make_adder(1)\n    plus10 = make_adder(10)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(-2) == 8, ('assertEqual', plus10(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testExtraNesting" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder2(x):\n\n        def extra():\n\n            def adder(y):\n                return x + y\n            return adder\n        return extra()\n    inc = make_adder2(1)\n    plus10 = make_adder2(10)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(-2) == 8, ('assertEqual', plus10(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testSimpleAndRebinding" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder3(x):\n\n        def adder(y):\n            return x + y\n        x = x + 1\n        return adder\n    inc = make_adder3(0)\n    plus10 = make_adder3(9)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(-2) == 8, ('assertEqual', plus10(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNestingGlobalNoFree" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder4():\n\n        def nest():\n\n            def nest():\n\n                def adder(y):\n                    return global_x + y\n                return adder\n            return nest()\n        return nest()\n    global_x = 1\n    adder = make_adder4()\n    assert adder(1) == 2, ('assertEqual', adder(1), 2)\n    global_x = 10\n    assert adder(-2) == 8, ('assertEqual', adder(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNestingThroughClass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder5(x):\n\n        class Adder:\n\n            def __call__(self, y):\n                return x + y\n        return Adder()\n    inc = make_adder5(1)\n    plus10 = make_adder5(10)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(-2) == 8, ('assertEqual', plus10(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNestingPlusFreeRefToGlobal" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def make_adder6(x):\n        global global_nest_x\n\n        def adder(y):\n            return global_nest_x + y\n        global_nest_x = x\n        return adder\n    inc = make_adder6(1)\n    plus10 = make_adder6(10)\n    assert inc(1) == 11, ('assertEqual', inc(1), 11)\n    assert plus10(-2) == 8, ('assertEqual', plus10(-2), 8)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNearestEnclosingScope" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def g(y):\n            x = 42\n\n            def h(z):\n                return x + z\n            return h\n        return g(2)\n    test_func = f(10)\n    assert test_func(5) == 47, ('assertEqual', test_func(5), 47)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testMixedFreevarsAndCellvars" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def identity(x):\n        return x\n\n    def f(x, y, z):\n\n        def g(a, b, c):\n            a = a + x\n\n            def h():\n                return identity(z * (b + y))\n            y = c + z\n            return h\n        return g\n    g = f(1, 2, 3)\n    h = g(2, 4, 6)\n    assert h() == 39, ('assertEqual', h(), 39)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testFreeVarInMethod" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def test():\n        method_and_var = 'var'\n\n        class Test:\n\n            def method_and_var(self):\n                return 'method'\n\n            def test(self):\n                return method_and_var\n\n            def actual_global(self):\n                return str('global')\n\n            def str(self):\n                return str(self)\n        return Test()\n    t = test()\n    assert t.test() == 'var', ('assertEqual', t.test(), 'var')\n    assert t.method_and_var() == 'method', ('assertEqual', t.method_and_var(), 'method')\n    assert t.actual_global() == 'global', ('assertEqual', t.actual_global(), 'global')\n    method_and_var = 'var'\n\n    class Test:\n\n        def method_and_var(self):\n            return 'method'\n\n        def test(self):\n            return method_and_var\n\n        def actual_global(self):\n            return str('global')\n\n        def str(self):\n            return str(self)\n    t = Test()\n    assert t.test() == 'var', ('assertEqual', t.test(), 'var')\n    assert t.method_and_var() == 'method', ('assertEqual', t.method_and_var(), 'method')\n    assert t.actual_global() == 'global', ('assertEqual', t.actual_global(), 'global')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testCellIsKwonlyArg" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def foo(*, a=17):\n\n        def bar():\n            return a + 5\n        return bar() + 3\n    assert foo(a=42) == 50, ('assertEqual', foo(a=42), 50)\n    assert foo() == 25, ('assertEqual', foo(), 25)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testCellIsArgAndEscapes" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def external():\n        value = 42\n\n        def inner():\n            return value\n        cell, = inner.__closure__\n        return cell\n    cell_ext = external()\n\n    def spam(arg):\n\n        def eggs():\n            return arg\n        return eggs\n    eggs = spam(cell_ext)\n    cell_closure, = eggs.__closure__\n    cell_eggs = eggs()\n    assert cell_eggs is cell_ext, ('assertIs', cell_eggs, cell_ext)\n    assert cell_eggs is not cell_closure, ('assertIsNot', cell_eggs, cell_closure)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testCellIsLocalAndEscapes" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def external():\n        value = 42\n\n        def inner():\n            return value\n        cell, = inner.__closure__\n        return cell\n    cell_ext = external()\n\n    def spam(arg):\n        cell = arg\n\n        def eggs():\n            return cell\n        return eggs\n    eggs = spam(cell_ext)\n    cell_closure, = eggs.__closure__\n    cell_eggs = eggs()\n    assert cell_eggs is cell_ext, ('assertIs', cell_eggs, cell_ext)\n    assert cell_eggs is not cell_closure, ('assertIsNot', cell_eggs, cell_closure)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testRecursion" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def fact(n):\n            if n == 0:\n                return 1\n            else:\n                return n * fact(n - 1)\n        if x >= 0:\n            return fact(x)\n        else:\n            raise ValueError('x must be >= 0')\n    assert f(6) == 720, ('assertEqual', f(6), 720)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testLambdas" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    f1 = lambda x: lambda y: x + y\n    inc = f1(1)\n    plus10 = f1(10)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(5) == 15, ('assertEqual', plus10(5), 15)\n    f2 = lambda x: (lambda: lambda y: x + y)()\n    inc = f2(1)\n    plus10 = f2(10)\n    assert inc(1) == 2, ('assertEqual', inc(1), 2)\n    assert plus10(5) == 15, ('assertEqual', plus10(5), 15)\n    f3 = lambda x: lambda y: global_x + y\n    global_x = 1\n    inc = f3(None)\n    assert inc(2) == 3, ('assertEqual', inc(2), 3)\n    f8 = lambda x, y, z: lambda a, b, c: lambda: z * (b + y)\n    g = f8(1, 2, 3)\n    h = g(2, 4, 6)\n    assert h() == 18, ('assertEqual', h(), 18)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testUnboundLocal" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def errorInOuter():\n        print(y)\n\n        def inner():\n            return y\n        y = 1\n\n    def errorInInner():\n\n        def inner():\n            return y\n        inner()\n        y = 1\n    try:\n        errorInOuter()\n    except UnboundLocalError:\n        pass\n    try:\n        errorInInner()\n    except NameError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testUnboundLocal_AfterDel" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def errorInOuter():\n        y = 1\n        del y\n        print(y)\n\n        def inner():\n            return y\n\n    def errorInInner():\n\n        def inner():\n            return y\n        y = 1\n        del y\n        inner()\n    try:\n        errorInOuter()\n    except UnboundLocalError:\n        pass\n    try:\n        errorInInner()\n    except NameError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testComplexDefinitions" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def makeReturner(*lst):\n\n        def returner():\n            return lst\n        return returner\n    assert makeReturner(1, 2, 3)() == (1, 2, 3), ('assertEqual', makeReturner(1, 2, 3)(), (1, 2, 3))\n\n    def makeReturner2(**kwargs):\n\n        def returner():\n            return kwargs\n        return returner\n    assert makeReturner2(a=11)()['a'] == 11, ('assertEqual', makeReturner2(a=11)()['a'], 11)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testLeaks" {
    (ok, detail) = p2_libtest_run_snippet("from test.support import gc_collect\n\ndef _t():\n\n    class Foo:\n        count = 0\n\n        def __init__(self):\n            Foo.count += 1\n\n        def __del__(self):\n            Foo.count -= 1\n\n    def f1():\n        x = Foo()\n\n        def f2():\n            return x\n        f2()\n    for i in range(100):\n        f1()\n    gc_collect()\n    assert Foo.count == 0, ('assertEqual', Foo.count, 0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testLocalsFunction" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def g(y):\n\n            def h(z):\n                return y + z\n            w = x + y\n            y += 3\n            return locals()\n        return g\n    d = f(2)(4)\n    assert 'h' in d, ('assertIn', 'h', d)\n    del d['h']\n    assert d == {'x': 2, 'y': 7, 'w': 6}, ('assertEqual', d, {'x': 2, 'y': 7, 'w': 6})\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testLocalsClass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        class C:\n            x = 12\n\n            def m(self):\n                return x\n            locals()\n        return C\n    assert f(1).x == 12, ('assertEqual', f(1).x, 12)\n\n    def f(x):\n\n        class C:\n            y = x\n\n            def m(self):\n                return x\n            z = list(locals())\n        return C\n    varnames = f(1).z\n    assert 'x' not in varnames, ('assertNotIn', 'x', varnames)\n    assert 'y' in varnames, ('assertIn', 'y', varnames)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testBoundAndFree" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        class C:\n\n            def m(self):\n                return x\n            a = x\n        return C\n    inst = f(3)()\n    assert inst.a == inst.m(), ('assertEqual', inst.a, inst.m())\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testEvalExecFreeVars" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n        return lambda: x + 1\n    g = f(3)\n    try:\n        eval(*(g.__code__,))\n    except TypeError:\n        pass\n    try:\n        exec(g.__code__, {})\n    except TypeError:\n        pass\n    else:\n        raise AssertionError('exec should have failed, because code contained free vars')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testListCompLocalVars" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    try:\n        print(bad)\n    except NameError:\n        pass\n    else:\n        print('bad should not be defined')\n\n    def x():\n        [bad for s in 'a b' for bad in s.split()]\n    x()\n    try:\n        print(bad)\n    except NameError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testEvalFreeVars" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def g():\n            x\n            eval('x + 1')\n        return g\n    f(4)()\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNonLocalFunction" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def inc():\n            nonlocal x\n            x += 1\n            return x\n\n        def dec():\n            nonlocal x\n            x -= 1\n            return x\n        return (inc, dec)\n    inc, dec = f(0)\n    assert inc() == 1, ('assertEqual', inc(), 1)\n    assert inc() == 2, ('assertEqual', inc(), 2)\n    assert dec() == 1, ('assertEqual', dec(), 1)\n    assert dec() == 0, ('assertEqual', dec(), 0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNonLocalMethod" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        class c:\n\n            def inc(self):\n                nonlocal x\n                x += 1\n                return x\n\n            def dec(self):\n                nonlocal x\n                x -= 1\n                return x\n        return c()\n    c = f(0)\n    assert c.inc() == 1, ('assertEqual', c.inc(), 1)\n    assert c.inc() == 2, ('assertEqual', c.inc(), 2)\n    assert c.dec() == 1, ('assertEqual', c.dec(), 1)\n    assert c.dec() == 0, ('assertEqual', c.dec(), 0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testGlobalInParallelNestedFunctions" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    local_ns = {}\n    global_ns = {}\n    exec('if 1:\\n            def f():\\n                y = 1\\n                def g():\\n                    global y\\n                    return y\\n                def h():\\n                    return y + 1\\n                return g, h\\n            y = 9\\n            g, h = f()\\n            result9 = g()\\n            result2 = h()\\n            ', local_ns, global_ns)\n    assert 2 == global_ns['result2'], ('assertEqual', 2, global_ns['result2'])\n    assert 9 == global_ns['result9'], ('assertEqual', 9, global_ns['result9'])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNonLocalClass" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        class c:\n            nonlocal x\n            x += 1\n\n            def get(self):\n                return x\n        return c()\n    c = f(0)\n    assert c.get() == 1, ('assertEqual', c.get(), 1)\n    assert 'x' not in c.__class__.__dict__, ('assertNotIn', 'x', c.__class__.__dict__)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNonLocalGenerator" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def g(y):\n            nonlocal x\n            for i in range(y):\n                x += 1\n                yield x\n        return g\n    g = f(0)\n    assert list(g(5)) == [1, 2, 3, 4, 5], ('assertEqual', list(g(5)), [1, 2, 3, 4, 5])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testNestedNonLocal" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f(x):\n\n        def g():\n            nonlocal x\n            x -= 2\n\n            def h():\n                nonlocal x\n                x += 4\n                return x\n            return h\n        return g\n    g = f(1)\n    h = g()\n    assert h() == 3, ('assertEqual', h(), 3)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testTopIsNotSignificant" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def top(a):\n        pass\n\n    def b():\n        global a\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.testCellLeak" {
    (ok, detail) = p2_libtest_run_snippet("import weakref\nfrom test.support import gc_collect\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n\n    class Tester:\n\n        def dig(self):\n            if 0:\n                lambda: self\n            try:\n                1 / 0\n            except Exception as exc:\n                self.exc = exc\n            self = None\n    tester = Tester()\n    tester.dig()\n    ref = weakref.ref(tester)\n    del tester\n    gc_collect()\n    assert ref() is None, ('assertIsNone', ref())\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ScopeTests.test_multiple_nesting" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class MultiplyNested:\n\n        def f1(self):\n            __arg = 1\n\n            class D:\n\n                def g(self, __arg):\n                    return __arg\n            return D().g(_MultiplyNested__arg=2)\n\n        def f2(self):\n            __arg = 1\n\n            class D:\n\n                def g(self, __arg):\n                    return __arg\n            return D().g\n    inst = MultiplyNested()\n    try:\n        inst.f1()\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    closure = inst.f2()\n    try:\n        closure(_MultiplyNested__arg=2)\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
