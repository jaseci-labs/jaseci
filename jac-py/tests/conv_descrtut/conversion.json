{
  "schema_version": 1,
  "tool_version": "conv_suite-0.4.0",
  "jac_sha": "f3db9fa7903fddf8f5d9a538402a7c734a573823",
  "cpython": {
    "version": "3.14.6",
    "sha": "unknown"
  },
  "command": [
    "convert_suite.py",
    "/home/jac/repos/jac-python/reference/cpython/Lib/test/test_descrtut.py",
    "-o",
    "/var/tmp/lane8/jac-py/tests/conv_descrtut",
    "--name",
    "conv_descrtut"
  ],
  "generated_at": "2026-08-24T14:07:17",
  "hashes": {
    "pins_jac": "776f1e5e8c450554100821dd7478bbdd07d73a0db6122bd8028297aa64b6a770"
  },
  "source_file": "/home/jac/repos/jac-python/reference/cpython/Lib/test/test_descrtut.py",
  "generator": "jac-py/tools/convert_suite.py",
  "module_stem": "descrtut",
  "pins_file": "conv_descrtut_pins.jac",
  "pins": [
    {
      "ident": "descrtut.doctests:tut1",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n_d_r = _d_print(defaultdict)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(0, \"<class '__main__.defaultdict'>\")\n_d_clear()\n_d_r = _d_print(type(defaultdict))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(1, \"<class 'type'>\")\n_d_clear()\na = defaultdict(default=0.0)\n_d_clear()\n_d_r = _d_print(a)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(3, '{}')\n_d_clear()\n_d_r = _d_print(type(a))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(4, \"<class '__main__.defaultdict'>\")\n_d_clear()\n_d_r = _d_print(a.__class__)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(5, \"<class '__main__.defaultdict'>\")\n_d_clear()\n_d_r = _d_print(type(a) is a.__class__)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(6, 'True')\n_d_clear()\na[1] = 3.25\n_d_clear()\n_d_r = _d_print(a)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(8, '{1: 3.25}')\n_d_clear()\n_d_r = _d_print(a[1])\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(9, '3.25')\n_d_clear()\n_d_r = _d_print(a[0])\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(10, '0.0')\n_d_clear()\n_d_r = a.merge({1: 100, 2: 200})\n_d_clear()\n_d_r = _d_print(sortdict(a))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(12, '{1: 3.25, 2: 200}')\n_d_clear()\n_d_r = _d_print(sorted(a.keys()))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(13, '[1, 2]')\n_d_clear()\na['print'] = _d_print\n_d_clear()\n_d_r = exec('x = 3; print(x)', a)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(15, '3')\n_d_clear()\n_d_r = _d_print(sorted(a.keys(), key=lambda x: (str(type(x)), x)))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(16, \"[1, 2, '__builtins__', 'print', 'x']\")\n_d_clear()\n_d_r = _d_print(a['x'])\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(17, '3')\n_d_clear()\na.default = -1\n_d_clear()\n_d_r = _d_print(a['noway'])\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(19, '-1')\n_d_clear()\na.default = -1000\n_d_clear()\n_d_r = _d_print(a['noway'])\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(21, '-1000')\n_d_clear()\n_d_r = 'default' in dir(a)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(22, 'True')\n_d_clear()\na.x1 = 100\n_d_clear()\na.x2 = 200\n_d_clear()\n_d_r = _d_print(a.x1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(25, '100')\n_d_clear()\nd = dir(a)\n_d_clear()\n_d_r = 'default' in d and 'x1' in d and ('x2' in d)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(27, 'True')\n_d_clear()\n_d_r = _d_print(sortdict(a.__dict__))\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(28, \"{'default': -1000, 'x1': 100, 'x2': 200}\")\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut2",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\na = defaultdict2(default=0.0)\n_d_clear()\n_d_r = a[1]\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(1, '0.0')\n_d_clear()\na.default = -1\n_d_clear()\n_d_r = a[1]\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(3, '-1')\ntry:\n    _d_clear()\n    a.x1 = 1\nexcept AttributeError as _d_e:\n    assert type(_d_e).__name__ + (': ' + (_d_e.msg if isinstance(_d_e, SyntaxError) else str(_d_e)) if (_d_e.msg if isinstance(_d_e, SyntaxError) else str(_d_e)) != '' else '') == \"AttributeError: 'defaultdict2' object has no attribute 'x1' and no __dict__ for setting new attributes\", ('doctest-exc4', _d_e)\nelse:\n    raise AssertionError('ex4: expected AttributeError')\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut3",
      "status": "quarantined",
      "reason": "harness-error:AssertionError: ('doctest', 7, '', \"['__add__',\\n '__class__',\\n '__class_getitem__',\\n '__contains__',\\n '__delattr__',"
    },
    {
      "ident": "descrtut.doctests:tut4",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n\nclass C:\n\n    @staticmethod\n    def foo(x, y):\n        _d_print('staticmethod', x, y)\n_d_clear()\n_d_r = C.foo(1, 2)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(1, 'staticmethod 1 2')\n_d_clear()\nc = C()\n_d_clear()\n_d_r = c.foo(1, 2)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(3, 'staticmethod 1 2')\n_d_clear()\n\nclass C:\n\n    @classmethod\n    def foo(cls, y):\n        _d_print('classmethod', cls, y)\n_d_clear()\n_d_r = C.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(5, \"classmethod <class '__main__.C'> 1\")\n_d_clear()\nc = C()\n_d_clear()\n_d_r = c.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(7, \"classmethod <class '__main__.C'> 1\")\n_d_clear()\n\nclass D(C):\n    pass\n_d_clear()\n_d_r = D.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(9, \"classmethod <class '__main__.D'> 1\")\n_d_clear()\nd = D()\n_d_clear()\n_d_r = d.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(11, \"classmethod <class '__main__.D'> 1\")\n_d_clear()\n\nclass E(C):\n\n    @classmethod\n    def foo(cls, y):\n        _d_print('E.foo() called')\n        C.foo(y)\n_d_clear()\n_d_r = E.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(13, \"E.foo() called\\nclassmethod <class '__main__.C'> 1\")\n_d_clear()\ne = E()\n_d_clear()\n_d_r = e.foo(1)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(15, \"E.foo() called\\nclassmethod <class '__main__.C'> 1\")\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut5",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n\nclass property(object):\n\n    def __init__(self, get, set=None):\n        self.__get = get\n        self.__set = set\n\n    def __get__(self, inst, type=None):\n        return self.__get(inst)\n\n    def __set__(self, inst, value):\n        if self.__set is None:\n            raise AttributeError('this attribute is read-only')\n        return self.__set(inst, value)\n_d_clear()\n\nclass C(object):\n\n    def __init__(self):\n        self.__x = 0\n\n    def getx(self):\n        return self.__x\n\n    def setx(self, x):\n        if x < 0:\n            x = 0\n        self.__x = x\n    x = property(getx, setx)\n_d_clear()\na = C()\n_d_clear()\na.x = 10\n_d_clear()\n_d_r = _d_print(a.x)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(4, '10')\n_d_clear()\na.x = -10\n_d_clear()\n_d_r = _d_print(a.x)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(6, '0')\n_d_clear()\ndel property\n_d_clear()\n_d_r = property\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(8, \"<class 'property'>\")\n_d_clear()\n\nclass C(object):\n\n    def __init__(self):\n        self.__x = 0\n\n    def getx(self):\n        return self.__x\n\n    def setx(self, x):\n        if x < 0:\n            x = 0\n        self.__x = x\n    x = property(getx, setx)\n_d_clear()\na = C()\n_d_clear()\na.x = 10\n_d_clear()\n_d_r = _d_print(a.x)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(12, '10')\n_d_clear()\na.x = -10\n_d_clear()\n_d_r = _d_print(a.x)\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(14, '0')\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut6",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n\nclass A:\n\n    def save(self):\n        _d_print('called A.save()')\n_d_clear()\n\nclass B(A):\n    pass\n_d_clear()\n\nclass C(A):\n\n    def save(self):\n        _d_print('called C.save()')\n_d_clear()\n\nclass D(B, C):\n    pass\n_d_clear()\n_d_r = D().save()\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(4, 'called C.save()')\n_d_clear()\n\nclass A(object):\n\n    def save(self):\n        _d_print('called A.save()')\n_d_clear()\n\nclass B(A):\n    pass\n_d_clear()\n\nclass C(A):\n\n    def save(self):\n        _d_print('called C.save()')\n_d_clear()\n\nclass D(B, C):\n    pass\n_d_clear()\n_d_r = D().save()\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(9, 'called C.save()')\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut7",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n_d_r = _d_print(D().m())\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(0, 'DCBA')\nprint('ok')\n"
    },
    {
      "ident": "descrtut.doctests:tut8",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test.support import sortdict\n\nclass defaultdict(dict):\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass defaultdict2(dict):\n    __slots__ = ['default']\n\n    def __init__(self, default=None):\n        dict.__init__(self)\n        self.default = default\n\n    def __getitem__(self, key):\n        try:\n            return dict.__getitem__(self, key)\n        except KeyError:\n            return self.default\n\n    def get(self, key, *args):\n        if not args:\n            args = (self.default,)\n        return dict.get(self, key, *args)\n\n    def merge(self, other):\n        for key in other:\n            if key not in self:\n                self[key] = other[key]\n\nclass A(object):\n\n    def m(self):\n        return 'A'\n\nclass B(A):\n\n    def m(self):\n        return 'B' + super(B, self).m()\n\nclass C(A):\n\n    def m(self):\n        return 'C' + super(C, self).m()\n\nclass D(C, B):\n\n    def m(self):\n        return 'D' + super(D, self).m()\n_d_buf = []\n\ndef _d_print(*args, sep=' '):\n    _d_buf.append(sep.join((str(a) for a in args)))\n\ndef _d_clear():\n    _d_buf.clear()\n\ndef _d_ell_match(want, got):\n    parts = want.split('...')\n    if len(parts) == 1:\n        return want == got\n    if not got.startswith(parts[0]) or not got.endswith(parts[-1]):\n        return False\n    pos = len(parts[0])\n    end = len(got) - len(parts[-1])\n    for seg in parts[1:-1]:\n        idx = got.find(seg, pos)\n        if idx < 0 or idx > end:\n            return False\n        pos = idx + len(seg)\n    return True\n\ndef _d_check(idx, want, ell=False):\n    got = chr(10).join(_d_buf)\n    ok = _d_ell_match(want, got) if ell else got == want\n    if not ok and want in ('0', '1'):\n        ok = got == ('False' if want == '0' else 'True')\n    assert ok, ('doctest', idx, got, want)\n_d_clear()\n\nclass A:\n\n    def foo(self):\n        _d_print('called A.foo()')\n_d_clear()\n\nclass B(A):\n    pass\n_d_clear()\n\nclass C(A):\n\n    def foo(self):\n        B.foo(self)\n_d_clear()\n_d_r = C().foo()\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(3, 'called A.foo()')\n_d_clear()\n\nclass C(A):\n\n    def foo(self):\n        A.foo(self)\n_d_clear()\n_d_r = C().foo()\nif _d_r is not None:\n    _d_buf.append(repr(_d_r))\n_d_check(5, 'called A.foo()')\nprint('ok')\n"
    }
  ],
  "quarantined": [],
  "counts": {
    "extracted": 8,
    "pinned": 7,
    "quarantined": 1
  }
}
