# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_secrets.py

test "Compare_Digest_Tests.test_equal" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    for s in ('a', 'bcd', 'xyz123'):\n        a = s * 100\n        b = s * 100\n        assert secrets.compare_digest(a, b), ('assertTrue', secrets.compare_digest(a, b))\n        assert secrets.compare_digest(a.encode('utf-8'), b.encode('utf-8')), ('assertTrue', secrets.compare_digest(a.encode('utf-8'), b.encode('utf-8')))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Compare_Digest_Tests.test_unequal" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    assert not secrets.compare_digest('abc', 'abcd'), ('assertFalse', secrets.compare_digest('abc', 'abcd'))\n    assert not secrets.compare_digest(b'abc', b'abcd'), ('assertFalse', secrets.compare_digest(b'abc', b'abcd'))\n    for s in ('x', 'mn', 'a1b2c3'):\n        a = s * 100 + 'q'\n        b = s * 100 + 'k'\n        assert not secrets.compare_digest(a, b), ('assertFalse', secrets.compare_digest(a, b))\n        assert not secrets.compare_digest(a.encode('utf-8'), b.encode('utf-8')), ('assertFalse', secrets.compare_digest(a.encode('utf-8'), b.encode('utf-8')))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Compare_Digest_Tests.test_bad_types" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    a = 'abcde'\n    b = a.encode('utf-8')\n    assert isinstance(a, str)\n    assert isinstance(b, bytes)\n    try:\n        secrets.compare_digest(*(a, b))\n    except TypeError:\n        pass\n    try:\n        secrets.compare_digest(*(b, a))\n    except TypeError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Compare_Digest_Tests.test_bool" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    assert isinstance(secrets.compare_digest('abc', 'abc'), bool), ('assertIsInstance', secrets.compare_digest('abc', 'abc'), bool)\n    assert isinstance(secrets.compare_digest('abc', 'xyz'), bool), ('assertIsInstance', secrets.compare_digest('abc', 'xyz'), bool)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Random_Tests.test_randbits" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    errmsg = 'randbits(%d) returned %d'\n    for numbits in (3, 12, 30):\n        for i in range(6):\n            n = secrets.randbits(numbits)\n            assert 0 <= n < 2 ** numbits, ('assertTrue', 0 <= n < 2 ** numbits)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Random_Tests.test_choice" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    items = [1, 2, 4, 8, 16, 32, 64]\n    for i in range(10):\n        assert secrets.choice(items) in items, ('assertTrue', secrets.choice(items) in items)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Random_Tests.test_randbelow" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    for i in range(2, 10):\n        assert secrets.randbelow(i) in range(i), ('assertIn', secrets.randbelow(i), range(i))\n    try:\n        secrets.randbelow(*(0,))\n    except ValueError:\n        pass\n    try:\n        secrets.randbelow(*(-1,))\n    except ValueError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Token_Tests.test_token_defaults" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    for func in (secrets.token_bytes, secrets.token_hex, secrets.token_urlsafe):\n        name = func.__name__\n        try:\n            func()\n        except TypeError:\n            raise AssertionError('%s cannot be called with no argument' % name)\n        try:\n            func(None)\n        except TypeError:\n            raise AssertionError('%s cannot be called with None' % name)\n    size = secrets.DEFAULT_ENTROPY\n    assert len(secrets.token_bytes(None)) == size, ('assertEqual', len(secrets.token_bytes(None)), size)\n    assert len(secrets.token_hex(None)) == 2 * size, ('assertEqual', len(secrets.token_hex(None)), 2 * size)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Token_Tests.test_token_bytes" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\n\ndef _t():\n    for n in (1, 8, 17, 100):\n        assert isinstance(secrets.token_bytes(n), bytes), ('assertIsInstance', secrets.token_bytes(n), bytes)\n        assert len(secrets.token_bytes(n)) == n, ('assertEqual', len(secrets.token_bytes(n)), n)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Token_Tests.test_token_hex" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\nimport string\n\ndef _t():\n    for n in (1, 12, 25, 90):\n        s = secrets.token_hex(n)\n        assert isinstance(s, str), ('assertIsInstance', s, str)\n        assert len(s) == 2 * n, ('assertEqual', len(s), 2 * n)\n        assert all((c in string.hexdigits for c in s)), ('assertTrue', all((c in string.hexdigits for c in s)))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "Token_Tests.test_token_urlsafe" {
    (ok, detail) = p2_libtest_run_snippet("import secrets\nimport string\n\ndef _t():\n    legal = string.ascii_letters + string.digits + '-_'\n    for n in (1, 11, 28, 76):\n        s = secrets.token_urlsafe(n)\n        assert isinstance(s, str), ('assertIsInstance', s, str)\n        assert all((c in legal for c in s)), ('assertTrue', all((c in legal for c in s)))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
