# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_textwrap.py

test "WrapTestCase.test_bad_width" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    text = \"Whatever, it doesn't matter.\"\n    try:\n        wrap(*(text, 0))\n    except ValueError:\n        pass\n    try:\n        wrap(*(text, -1))\n    except ValueError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "DedentTestCase.test_type_error" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nfrom textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    try:\n        dedent(0)\n    except TypeError as _exc:\n        assert _re.search('expected str object, not', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        dedent(b'')\n    except TypeError as _exc:\n        assert _re.search('expected str object, not', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "DedentTestCase.test_dedent_even" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    text = '  Hello there.\\n  How are ya?\\n  Oh good.'\n    expect = 'Hello there.\\nHow are ya?\\nOh good.'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '  Hello there.\\n\\n  How are ya?\\n  Oh good.\\n'\n    expect = 'Hello there.\\n\\nHow are ya?\\nOh good.\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '  Hello there.\\n  \\n  How are ya?\\n  Oh good.\\n'\n    expect = 'Hello there.\\n\\nHow are ya?\\nOh good.\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "DedentTestCase.test_dedent_uneven" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    text = '        def foo():\\n            while 1:\\n                return foo\\n        '\n    expect = 'def foo():\\n    while 1:\\n        return foo\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '  Foo\\n    Bar\\n\\n   Baz\\n'\n    expect = 'Foo\\n  Bar\\n\\n Baz\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '  Foo\\n    Bar\\n \\n   Baz\\n'\n    expect = 'Foo\\n  Bar\\n\\n Baz\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "DedentTestCase.test_dedent_declining" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    text = '     Foo\\n    Bar\\n'\n    expect = ' Foo\\nBar\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '     Foo\\n\\n    Bar\\n'\n    expect = ' Foo\\n\\nBar\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    text = '     Foo\\n    \\n    Bar\\n'\n    expect = ' Foo\\n\\nBar\\n'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "DedentTestCase.test_dedent_preserve_internal_tabs" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    text = '  hello\\tthere\\n  how are\\tyou?'\n    expect = 'hello\\tthere\\nhow are\\tyou?'\n    assert expect == dedent(text), ('assertEqual', expect, dedent(text))\n    assert expect == dedent(expect), ('assertEqual', expect, dedent(expect))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ShortenTestCase.test_width_too_small_for_placeholder" {
    (ok, detail) = p2_libtest_run_snippet("from textwrap import TextWrapper, wrap, fill, dedent, indent, shorten\n\ndef _t():\n    shorten('x' * 20, width=8, placeholder='(......)')\n    try:\n        shorten('x' * 20, width=8, placeholder='(.......)')\n    except ValueError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
