{
  "schema_version": 1,
  "tool_version": "conv_suite-0.4.0",
  "jac_sha": "4e27b6c6bce510c5490cc3e90b0fe5c36165f1f0",
  "cpython": {
    "version": "3.14.6",
    "sha": "unknown"
  },
  "command": [
    "convert_suite.py",
    "/home/jac/repos/jac-python/reference/cpython/Lib/test/test_pstats.py",
    "-o",
    "/var/tmp/lane8/jac-py/tests/conv_pstats",
    "--name",
    "conv_pstats"
  ],
  "generated_at": "2026-08-24T21:38:50",
  "hashes": {
    "pins_jac": "cd545f15809d2c11d19e94a4ca45d87e802208aa5264022c969f40427d63c0a4"
  },
  "source_file": "/home/jac/repos/jac-python/reference/cpython/Lib/test/test_pstats.py",
  "generator": "jac-py/tools/convert_suite.py",
  "module_stem": "pstats",
  "pins_file": "conv_pstats_pins.jac",
  "pins": [
    {
      "ident": "AddCallersTestCase.test_combine_results",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "import pstats\n\ndef _t():\n    target = {'a': (1, 2, 3, 4)}\n    source = {'a': (1, 2, 3, 4), 'b': (5, 6, 7, 8)}\n    new_callers = pstats.add_callers(target, source)\n    assert new_callers == {'a': (2, 4, 6, 8), 'b': (5, 6, 7, 8)}, ('assertEqual', new_callers, {'a': (2, 4, 6, 8), 'b': (5, 6, 7, 8)})\n    target = {'a': 1}\n    source = {'a': 1, 'b': 5}\n    new_callers = pstats.add_callers(target, source)\n    assert new_callers == {'a': 2, 'b': 5}, ('assertEqual', new_callers, {'a': 2, 'b': 5})\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_add",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nfrom io import StringIO\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    stream = StringIO()\n    stats = pstats.Stats(stream=stream)\n    stats.add(self.stats, self.stats)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_dump_and_load_works_correctly",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport os\nimport pstats\nimport tempfile\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    temp_storage_new = tempfile.NamedTemporaryFile(delete=False)\n    try:\n        self.stats.dump_stats(filename=temp_storage_new.name)\n        tmp_stats = pstats.Stats(temp_storage_new.name)\n        assert self.stats.stats == tmp_stats.stats, ('assertEqual', self.stats.stats, tmp_stats.stats)\n    finally:\n        temp_storage_new.close()\n        os.remove(temp_storage_new.name)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_load_equivalent_to_init",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport os\nimport pstats\nimport tempfile\nimport cProfile\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    stats = pstats.Stats()\n    self.temp_storage = tempfile.NamedTemporaryFile(delete=False)\n    try:\n        cProfile.run('import os', filename=self.temp_storage.name)\n        stats.load_stats(self.temp_storage.name)\n        created = pstats.Stats(self.temp_storage.name)\n        assert stats.stats == created.stats, ('assertEqual', stats.stats, created.stats)\n    finally:\n        self.temp_storage.close()\n        os.remove(self.temp_storage.name)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_loading_wrong_types",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    stats = pstats.Stats()\n    try:\n        stats.load_stats(42)\n    except TypeError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_sort_stats_int",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    valid_args = {-1: 'stdname', 0: 'calls', 1: 'time', 2: 'cumulative'}\n    for arg_int, arg_str in valid_args.items():\n        self.stats.sort_stats(arg_int)\n        assert self.stats.sort_type == self.stats.sort_arg_dict_default[arg_str][-1], ('assertEqual', self.stats.sort_type, self.stats.sort_arg_dict_default[arg_str][-1])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_sort_stats_string",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    for sort_name in ['calls', 'ncalls', 'cumtime', 'cumulative', 'filename', 'line', 'module', 'name', 'nfl', 'pcalls', 'stdname', 'time', 'tottime']:\n        self.stats.sort_stats(sort_name)\n        assert self.stats.sort_type == self.stats.sort_arg_dict_default[sort_name][-1], ('assertEqual', self.stats.sort_type, self.stats.sort_arg_dict_default[sort_name][-1])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_sort_stats_partial",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    sortkey = 'filename'\n    for sort_name in ['f', 'fi', 'fil', 'file', 'filen', 'filena', 'filenam', 'filename']:\n        self.stats.sort_stats(sort_name)\n        assert self.stats.sort_type == self.stats.sort_arg_dict_default[sortkey][-1], ('assertEqual', self.stats.sort_type, self.stats.sort_arg_dict_default[sortkey][-1])\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_sort_stats_enum",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nfrom pstats import SortKey\nfrom enum import StrEnum, _test_simple_enum\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    for member in SortKey:\n        self.stats.sort_stats(member)\n        assert self.stats.sort_type == self.stats.sort_arg_dict_default[member.value][-1], ('assertEqual', self.stats.sort_type, self.stats.sort_arg_dict_default[member.value][-1])\n\n    class CheckedSortKey(StrEnum):\n        CALLS = ('calls', 'ncalls')\n        CUMULATIVE = ('cumulative', 'cumtime')\n        FILENAME = ('filename', 'module')\n        LINE = 'line'\n        NAME = 'name'\n        NFL = 'nfl'\n        PCALLS = 'pcalls'\n        STDNAME = 'stdname'\n        TIME = ('time', 'tottime')\n\n        def __new__(cls, *values):\n            value = values[0]\n            obj = str.__new__(cls, value)\n            obj._value_ = value\n            for other_value in values[1:]:\n                cls._value2member_map_[other_value] = obj\n            obj._all_values = values\n            return obj\n    _test_simple_enum(CheckedSortKey, SortKey)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_sort_starts_mix",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nfrom pstats import SortKey\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    try:\n        self.stats.sort_stats(*('calls', SortKey.TIME))\n    except TypeError:\n        pass\n    try:\n        self.stats.sort_stats(*(SortKey.TIME, 'calls'))\n    except TypeError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_get_stats_profile",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nimport pstats\nimport cProfile\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\n    def pass1():\n        pass\n\n    def pass2():\n        pass\n\n    def pass3():\n        pass\n    pr = cProfile.Profile()\n    pr.enable()\n    pass1()\n    pass2()\n    pass3()\n    pr.create_stats()\n    ps = pstats.Stats(pr)\n    stats_profile = ps.get_stats_profile()\n    funcs_called = set(stats_profile.func_profiles.keys())\n    assert 'pass1' in funcs_called, ('assertIn', 'pass1', funcs_called)\n    assert 'pass2' in funcs_called, ('assertIn', 'pass2', funcs_called)\n    assert 'pass3' in funcs_called, ('assertIn', 'pass3', funcs_called)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    },
    {
      "ident": "StatsTestCase.test_SortKey_enum",
      "status": "pinned",
      "oracle": {
        "status": "ok"
      },
      "snippet": "from test import support\nfrom pstats import SortKey\nimport pstats\n\ndef setUp():\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n    stats_file = support.findfile('pstats.pck')\n    self.stats = pstats.Stats(stats_file)\n    assert SortKey.FILENAME == 'filename', ('assertEqual', SortKey.FILENAME, 'filename')\n    assert SortKey.FILENAME != SortKey.CALLS, ('assertNotEqual', SortKey.FILENAME, SortKey.CALLS)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n"
    }
  ],
  "quarantined": [
    {
      "ident": "LazyImportTest.test_lazy_import",
      "reason": "decorator:support.cpython_only"
    }
  ],
  "counts": {
    "extracted": 13,
    "pinned": 12,
    "quarantined": 1
  }
}
