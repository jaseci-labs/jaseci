# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_array.py

test "MiscTest.test_array_is_sequence" {
    (ok, detail) = p2_libtest_run_snippet("import collections.abc\nimport array\n\ndef _t():\n    assert isinstance(array.array('B'), collections.abc.MutableSequence), ('assertIsInstance', array.array('B'), collections.abc.MutableSequence)\n    assert isinstance(array.array('B'), collections.abc.Reversible), ('assertIsInstance', array.array('B'), collections.abc.Reversible)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "MiscTest.test_bad_constructor" {
    (ok, detail) = p2_libtest_run_snippet("import array\n\ndef _t():\n    try:\n        array.array()\n    except TypeError:\n        pass\n    try:\n        array.array(spam=42)\n    except TypeError:\n        pass\n    try:\n        array.array(*('xx',))\n    except TypeError:\n        pass\n    try:\n        array.array(*('x',))\n    except ValueError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "MiscTest.test_empty" {
    (ok, detail) = p2_libtest_run_snippet("import array\n\ndef _t():\n    a = array.array('B')\n    a[:] = a\n    assert len(a) == 0, ('assertEqual', len(a), 0)\n    assert len(a + a) == 0, ('assertEqual', len(a + a), 0)\n    assert len(a * 3) == 0, ('assertEqual', len(a * 3), 0)\n    a += a\n    assert len(a) == 0, ('assertEqual', len(a), 0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "LargeArrayTest.test_gh_128961" {
    (ok, detail) = p2_libtest_run_snippet("import array\n\ndef _t():\n    a = array.array('i')\n    it = iter(a)\n    list(it)\n    it.__setstate__(0)\n    try:\n        next(*(it,))\n    except StopIteration:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "LargeArrayTest.test_setitem_use_after_shrink_with_int_data" {
    (ok, detail) = p2_libtest_run_snippet("import array\n\ndef _t():\n    victim = array.array('b', [1, 2, 3])\n\n    class Index:\n\n        def __index__(self):\n            victim.pop()\n            victim.pop()\n            return 0\n    try:\n        victim.__setitem__(*(1, Index()))\n    except IndexError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
