# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_getpass.py

test "GetpassRawinputTest.test_flushes_stream_after_prompt" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\nfrom io import BytesIO, StringIO, TextIOWrapper\nfrom unittest import mock\n\ndef _t():\n    stream = mock.Mock(spec=StringIO)\n    input = StringIO('input_string')\n    getpass._raw_input('some_prompt', stream, input=input)\n    stream.flush.assert_called_once_with()\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "GetpassRawinputTest.test_uses_stderr_as_default" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\nfrom io import BytesIO, StringIO, TextIOWrapper\nfrom unittest import mock\n\ndef _t():\n    input = StringIO('input_string')\n    prompt = 'some_prompt'\n    with mock.patch('sys.stderr') as stderr:\n        getpass._raw_input(prompt, input=input)\n        stderr.write.assert_called_once_with(prompt)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "GetpassRawinputTest.test_raises_on_empty_input" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\nfrom io import BytesIO, StringIO, TextIOWrapper\n\ndef _t():\n    input = StringIO('')\n    try:\n        getpass._raw_input(input=input)\n    except EOFError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "GetpassRawinputTest.test_trims_trailing_newline" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\nfrom io import BytesIO, StringIO, TextIOWrapper\n\ndef _t():\n    input = StringIO('test\\n')\n    assert 'test' == getpass._raw_input(input=input), ('assertEqual', 'test', getpass._raw_input(input=input))\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "GetpassEchoCharTest.test_accept_none" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\n\ndef _t():\n    getpass._check_echo_char(None)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "GetpassEchoCharTest.test_reject_empty_string" {
    (ok, detail) = p2_libtest_run_snippet("import getpass\n\ndef _t():\n    try:\n        getpass.getpass(echo_char='')\n    except ValueError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
