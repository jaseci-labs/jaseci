# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_generator_stop.py

test "TestPEP479.test_stopiteration_wrapping" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\n\ndef _t():\n\n    def f():\n        raise StopIteration\n\n    def g():\n        yield f()\n    try:\n        next(g())\n    except RuntimeError as _exc:\n        assert _re.search('generator raised StopIteration', str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestPEP479.test_stopiteration_wrapping_context" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f():\n        raise StopIteration\n\n    def g():\n        yield f()\n    try:\n        next(g())\n    except RuntimeError as exc:\n        assert type(exc.__cause__) is StopIteration, ('assertIs', type(exc.__cause__), StopIteration)\n        assert type(exc.__context__) is StopIteration, ('assertIs', type(exc.__context__), StopIteration)\n        assert exc.__suppress_context__, ('assertTrue', exc.__suppress_context__)\n    else:\n        raise AssertionError('__cause__, __context__, or __suppress_context__ were not properly set')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
