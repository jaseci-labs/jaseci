# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_augassign.py

test "AugAssignTest.testBasic" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    x = 2\n    x += 1\n    x *= 2\n    x **= 2\n    x -= 8\n    x //= 5\n    x %= 3\n    x &= 2\n    x |= 5\n    x ^= 1\n    x /= 2\n    assert x == 3.0, ('assertEqual', x, 3.0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AugAssignTest.test_with_unpacking" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    try:\n        compile(*('x, b += 3', '<test>', 'exec'))\n    except SyntaxError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AugAssignTest.testInList" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    x = [2]\n    x[0] += 1\n    x[0] *= 2\n    x[0] **= 2\n    x[0] -= 8\n    x[0] //= 5\n    x[0] %= 3\n    x[0] &= 2\n    x[0] |= 5\n    x[0] ^= 1\n    x[0] /= 2\n    assert x[0] == 3.0, ('assertEqual', x[0], 3.0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AugAssignTest.testInDict" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    x = {0: 2}\n    x[0] += 1\n    x[0] *= 2\n    x[0] **= 2\n    x[0] -= 8\n    x[0] //= 5\n    x[0] %= 3\n    x[0] &= 2\n    x[0] |= 5\n    x[0] ^= 1\n    x[0] /= 2\n    assert x[0] == 3.0, ('assertEqual', x[0], 3.0)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AugAssignTest.testSequences" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    x = [1, 2]\n    x += [3, 4]\n    x *= 2\n    assert x == [1, 2, 3, 4, 1, 2, 3, 4], ('assertEqual', x, [1, 2, 3, 4, 1, 2, 3, 4])\n    x = [1, 2, 3]\n    y = x\n    x[1:2] *= 2\n    y[1:2] += [1]\n    assert x == [1, 2, 1, 2, 3], ('assertEqual', x, [1, 2, 1, 2, 3])\n    assert x is y, ('assertTrue', x is y)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
