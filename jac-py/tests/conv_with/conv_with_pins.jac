# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_with.py

test "FailureTestCase.testEnterAttributeError" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport re\n\ndef do_with(obj):\n    with obj:\n        pass\n\ndef _t():\n\n    class LacksEnter:\n\n        def __exit__(self, type, value, traceback):\n            ...\n    try:\n        do_with(LacksEnter())\n    except TypeError as _exc:\n        assert _re.search(re.escape('object does not support the context manager protocol (missed __enter__ method)'), str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testExitAttributeError" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport re\n\ndef do_with(obj):\n    with obj:\n        pass\n\ndef _t():\n\n    class LacksExit:\n\n        def __enter__(self):\n            ...\n    msg = re.escape('object does not support the context manager protocol (missed __exit__ method)')\n    try:\n        do_with(object())\n    except TypeError as _exc:\n        assert _re.search(msg, str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\n    try:\n        do_with(LacksExit())\n    except TypeError as _exc:\n        assert _re.search(msg, str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testWithForAsyncManager" {
    (ok, detail) = p2_libtest_run_snippet("import re as _re\nimport re\n\ndef do_with(obj):\n    with obj:\n        pass\n\ndef _t():\n\n    class AsyncManager:\n\n        async def __aenter__(self):\n            ...\n\n        async def __aexit__(self, type, value, traceback):\n            ...\n    try:\n        do_with(AsyncManager())\n    except TypeError as _exc:\n        assert _re.search(re.escape(\"object does not support the context manager protocol (missed __exit__ method) but it supports the asynchronous context manager protocol. Did you mean to use 'async with'?\"), str(_exc)), 'assertRaisesRegex: message mismatch'\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testAssignmentToNoneError" {
    (ok, detail) = p2_libtest_run_snippet("def assertRaisesSyntaxError(codestr):\n\n    def shouldRaiseSyntaxError(s):\n        compile(s, '', 'single')\n    try:\n        shouldRaiseSyntaxError(*(codestr,))\n    except SyntaxError:\n        pass\n\ndef _t():\n    assertRaisesSyntaxError('with mock as None:\\n  pass')\n    assertRaisesSyntaxError('with mock as (None):\\n  pass')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testAssignmentToTupleOnlyContainingNoneError" {
    (ok, detail) = p2_libtest_run_snippet("def assertRaisesSyntaxError(codestr):\n\n    def shouldRaiseSyntaxError(s):\n        compile(s, '', 'single')\n    try:\n        shouldRaiseSyntaxError(*(codestr,))\n    except SyntaxError:\n        pass\n\ndef _t():\n    assertRaisesSyntaxError('with mock as None,:\\n  pass')\n    assertRaisesSyntaxError('with mock as (None,):\\n  pass')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testAssignmentToTupleContainingNoneError" {
    (ok, detail) = p2_libtest_run_snippet("def assertRaisesSyntaxError(codestr):\n\n    def shouldRaiseSyntaxError(s):\n        compile(s, '', 'single')\n    try:\n        shouldRaiseSyntaxError(*(codestr,))\n    except SyntaxError:\n        pass\n\ndef _t():\n    assertRaisesSyntaxError('with mock as (foo, None, bar):\\n  pass')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FailureTestCase.testExitThrows" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class ExitThrows(object):\n\n        def __enter__(self):\n            return\n\n        def __exit__(self, *args):\n            raise RuntimeError(42)\n\n    def shouldThrow():\n        with ExitThrows():\n            pass\n    try:\n        shouldThrow()\n    except RuntimeError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AssignmentTargetTestCase.testMultipleComplexTargets" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class C:\n\n        def __enter__(self):\n            return (1, 2, 3)\n\n        def __exit__(self, t, v, tb):\n            pass\n    targets = {1: [0, 1, 2]}\n    with C() as (targets[1][0], targets[1][1], targets[1][2]):\n        assert targets == {1: [1, 2, 3]}, ('assertEqual', targets, {1: [1, 2, 3]})\n    with C() as (list(targets.values())[0][2], list(targets.values())[0][1], list(targets.values())[0][0]):\n        assert targets == {1: [3, 2, 1]}, ('assertEqual', targets, {1: [3, 2, 1]})\n    with C() as (targets[1], targets[2], targets[3]):\n        assert targets == {1: 1, 2: 2, 3: 3}, ('assertEqual', targets, {1: 1, 2: 2, 3: 3})\n\n    class B:\n        pass\n    blah = B()\n    with C() as (blah.one, blah.two, blah.three):\n        assert blah.one == 1, ('assertEqual', blah.one, 1)\n        assert blah.two == 2, ('assertEqual', blah.two, 2)\n        assert blah.three == 3, ('assertEqual', blah.three, 3)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "AssignmentTargetTestCase.testWithExtendedTargets" {
    (ok, detail) = p2_libtest_run_snippet("from contextlib import _GeneratorContextManager, contextmanager, nullcontext\n\ndef _t():\n    with nullcontext(range(1, 5)) as (a, *b, c):\n        assert a == 1, ('assertEqual', a, 1)\n        assert b == [2, 3], ('assertEqual', b, [2, 3])\n        assert c == 4, ('assertEqual', c, 4)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ExitSwallowsExceptionTestCase.testExitTrueSwallowsException" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class AfricanSwallow:\n\n        def __enter__(self):\n            pass\n\n        def __exit__(self, t, v, tb):\n            return True\n    try:\n        with AfricanSwallow():\n            1 / 0\n    except ZeroDivisionError:\n        raise AssertionError('ZeroDivisionError should have been swallowed')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "ExitSwallowsExceptionTestCase.testExitFalseDoesntSwallowException" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    class EuropeanSwallow:\n\n        def __enter__(self):\n            pass\n\n        def __exit__(self, t, v, tb):\n            return False\n    try:\n        with EuropeanSwallow():\n            1 / 0\n    except ZeroDivisionError:\n        pass\n    else:\n        raise AssertionError('ZeroDivisionError should have been raised')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
