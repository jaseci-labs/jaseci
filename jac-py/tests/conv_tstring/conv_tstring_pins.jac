# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_tstring.py

test "TestTString.test_string_representation" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    t = t'Hello'\n    assert repr(t) == \"Template(strings=('Hello',), interpolations=())\", ('assertEqual', repr(t), \"Template(strings=('Hello',), interpolations=())\")\n    name = 'Python'\n    t = t'Hello, {name}'\n    assert repr(t) == \"Template(strings=('Hello, ', ''), interpolations=(Interpolation('Python', 'name', None, ''),))\", ('assertEqual', repr(t), \"Template(strings=('Hello, ', ''), interpolations=(Interpolation('Python', 'name', None, ''),))\")\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestTString.test_nested_templates" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    name = 'Python'\n    inner = t'{name}'\n    t = t'Language: {inner}'\n    t_interp = t.interpolations[0]\n    assert t.strings == ('Language: ', ''), ('assertEqual', t.strings, ('Language: ', ''))\n    assert t_interp.value.strings == ('', ''), ('assertEqual', t_interp.value.strings, ('', ''))\n    assert t_interp.value.interpolations[0].value == name, ('assertEqual', t_interp.value.interpolations[0].value, name)\n    assert t_interp.value.interpolations[0].expression == 'name', ('assertEqual', t_interp.value.interpolations[0].expression, 'name')\n    assert t_interp.value.interpolations[0].conversion == None, ('assertEqual', t_interp.value.interpolations[0].conversion, None)\n    assert t_interp.value.interpolations[0].format_spec == '', ('assertEqual', t_interp.value.interpolations[0].format_spec, '')\n    assert t_interp.expression == 'inner', ('assertEqual', t_interp.expression, 'inner')\n    assert t_interp.conversion == None, ('assertEqual', t_interp.conversion, None)\n    assert t_interp.format_spec == '', ('assertEqual', t_interp.format_spec, '')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestTString.test_runtime_errors" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    try:\n        eval(\"t'Hello, {name}'\")\n    except NameError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
