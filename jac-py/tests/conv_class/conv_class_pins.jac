# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_class.py

test "TestInlineValues.test_store_attr_deleted_dict" {
    (ok, detail) = p2_libtest_run_snippet("callLst = []\n\ndef trackCall(f):\n\n    def track(*args, **kwargs):\n        callLst.append((f.__name__, args))\n        return f(*args, **kwargs)\n    return track\n\ndef _t():\n\n    class Foo:\n        pass\n    f = Foo()\n    del f.__dict__\n    f.a = 3\n    assert f.a == 3, ('assertEqual', f.a, 3)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestInlineValues.test_rematerialize_object_dict" {
    (ok, detail) = p2_libtest_run_snippet("callLst = []\n\ndef trackCall(f):\n\n    def track(*args, **kwargs):\n        callLst.append((f.__name__, args))\n        return f(*args, **kwargs)\n    return track\n\ndef _t():\n\n    class Foo:\n        pass\n    f = Foo()\n    f.__dict__['attr'] = 1\n    del f.__dict__\n\n    class StrSubclass(str):\n        pass\n    assert not hasattr(f, StrSubclass('attr')), ('assertFalse', hasattr(f, StrSubclass('attr')))\n\n    class Bar:\n        pass\n    f.__class__ = Bar\n    assert isinstance(f, Bar), ('assertIsInstance', f, Bar)\n    assert f.__dict__ == {}, ('assertEqual', f.__dict__, {})\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
