# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_call.py

test "FunctionCalls.test_kwargs_order" {
    (ok, detail) = p2_libtest_run_snippet("import collections\n\ndef testfunction(self):\n    \"\"\"some doc\"\"\"\n    return self\n\ndef testfunction_kw(self, *, kw):\n    \"\"\"some doc\"\"\"\n    return self\n\ndef _t():\n    od = collections.OrderedDict([('a', 1), ('b', 2)])\n    od.move_to_end('a')\n    expected = list(od.items())\n\n    def fn(**kw):\n        return kw\n    res = fn(**od)\n    assert isinstance(res, dict), ('assertIsInstance', res, dict)\n    assert list(res.items()) == expected, ('assertEqual', list(res.items()), expected)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FunctionCalls.test_frames_are_popped_after_failed_calls" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def f():\n        pass\n\n    class C:\n\n        def m(self):\n            pass\n    callables = [f, C.m, [].__len__]\n    for c in callables:\n        for _ in range(1000):\n            try:\n                c(None)\n            except TypeError:\n                pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "FastCallTests.test_fastcall_clearing_dict" {
    (ok, detail) = p2_libtest_run_snippet("import gc\n\ndef testfunction(self):\n    \"\"\"some doc\"\"\"\n    return self\n\ndef testfunction_kw(self, *, kw):\n    \"\"\"some doc\"\"\"\n    return self\n\ndef _t():\n\n    class _SelfNS:\n        pass\n    self = _SelfNS()\n\n    class IntWithDict:\n        __slots__ = ['kwargs']\n\n        def __init__(self, **kwargs):\n            self.kwargs = kwargs\n\n        def __index__(self):\n            self.kwargs.clear()\n            gc.collect()\n            return 0\n    x = IntWithDict(optimize=IntWithDict())\n    compile('pass', '', 'exec', x, **x.kwargs)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestRecursion.test_recursion_with_kwargs" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n\n    def recurse_kw(a=0):\n        recurse_kw(a=0)\n    try:\n        recurse_kw()\n    except RecursionError as _exc:\n        pass\n    else:\n        raise AssertionError('assertRaises: did not raise')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestFunctionWithManyArgs.test_function_with_many_args" {
    (ok, detail) = p2_libtest_run_snippet("def _t():\n    for N in (10, 500, 1000):\n        args = ','.join([f'a{i}' for i in range(N)])\n        src = f'def f({args}) : return a{N // 2}'\n        l = {}\n        exec(src, {}, l)\n        assert l['f'](*range(N)) == N // 2, ('assertEqual', l['f'](*range(N)), N // 2)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
