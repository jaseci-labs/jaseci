# Generated by jac-py/tools/convert_suite.py — DO NOT EDIT BY HAND.
# Output-oracle pins: every snippet replays its CPython Lib/test method on
# jacpython's ceval via layer_p2_libtest and asserts the host-captured
# outcome (host oracle captured at generation time).
import from layer_p2_libtest { p2_libtest_run_snippet }


# Source: test_shelve.py

test "TestCase.test_close" {
    (ok, detail) = p2_libtest_run_snippet("import shelve\n\ndef L1(s):\n    return s.decode('latin-1')\n\ndef _t():\n    d1 = {}\n    s = shelve.Shelf(d1, protocol=2, writeback=False)\n    s['key1'] = [1, 2, 3, 4]\n    assert s['key1'] == [1, 2, 3, 4], ('assertEqual', s['key1'], [1, 2, 3, 4])\n    assert len(s) == 1, ('assertEqual', len(s), 1)\n    s.close()\n    try:\n        len(*(s,))\n    except ValueError:\n        pass\n    try:\n        s['key1']\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('Closed shelf should not find a key')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestCase.test_keyencoding" {
    (ok, detail) = p2_libtest_run_snippet("import shelve\n\ndef L1(s):\n    return s.decode('latin-1')\n\ndef _t():\n    d = {}\n    key = 'P\u00c3\u00b6p'\n    shelve.Shelf(d)[key] = [1]\n    assert key.encode('utf-8') in d, ('assertIn', key.encode('utf-8'), d)\n    shelve.Shelf(d, keyencoding='latin-1')[key] = [1]\n    assert key.encode('latin-1') in d, ('assertIn', key.encode('latin-1'), d)\n    s = shelve.Shelf(d, keyencoding='ascii')\n    try:\n        s.__setitem__(*(key, [1]))\n    except UnicodeEncodeError:\n        pass\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestCase.test_writeback_also_writes_immediately" {
    (ok, detail) = p2_libtest_run_snippet("import shelve\n\ndef L1(s):\n    return s.decode('latin-1')\n\ndef _t():\n    d = {}\n    key = 'key'\n    encodedkey = key.encode('utf-8')\n    with shelve.Shelf(d, writeback=True) as s:\n        s[key] = [1]\n        p1 = d[encodedkey]\n        s['key'].append(2)\n    p2 = d[encodedkey]\n    assert p1 != p2, ('assertNotEqual', p1, p2)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestCase.test_with" {
    (ok, detail) = p2_libtest_run_snippet("import shelve\n\ndef L1(s):\n    return s.decode('latin-1')\n\ndef _t():\n    d1 = {}\n    with shelve.Shelf(d1, protocol=2, writeback=False) as s:\n        s['key1'] = [1, 2, 3, 4]\n        assert s['key1'] == [1, 2, 3, 4], ('assertEqual', s['key1'], [1, 2, 3, 4])\n        assert len(s) == 1, ('assertEqual', len(s), 1)\n    try:\n        len(*(s,))\n    except ValueError:\n        pass\n    try:\n        s['key1']\n    except ValueError:\n        pass\n    else:\n        raise AssertionError('Closed shelf should not find a key')\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}

test "TestCase.test_default_protocol" {
    (ok, detail) = p2_libtest_run_snippet("import shelve\nimport pickle\n\ndef L1(s):\n    return s.decode('latin-1')\n\ndef _t():\n    with shelve.Shelf({}) as s:\n        assert s._protocol == pickle.DEFAULT_PROTOCOL, ('assertEqual', s._protocol, pickle.DEFAULT_PROTOCOL)\ntry:\n    _t()\nexcept BaseException as _e:\n    print('ORACLE_EXC ' + type(_e).__name__ + ' ' + repr(str(_e)))\nelse:\n    print('ok')\n");
    assert ok , "RUN<" + detail + ">";
    assert detail == "ok" , "GOT<" + detail + ">";
}
