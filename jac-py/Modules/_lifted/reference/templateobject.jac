# P3.2e — template helpers.
# Lift: Objects/_lifted/templateobject_core.jac

def template_nstrings(n: int) -> int {
    return n;
}

def template_ninterpolations(n: int) -> int {
    return n;
}

def template_is_empty(nstrings: int) -> bool {
    return nstrings == 0;
}
