"""Argument Clinic → Jac signatures.

GENERATED by jac-py/tools/clinic2jac.py from jac-py/tools/p3_object_core/clinic_fixtures/bool_new.c
(CPython pin: see CURRENT.md). Do not edit by hand — regenerate instead.
"""

def bool_new_impl(type: PyTypeObject, object: PyObj | None = None) -> PyObj {
    x = object if object is not None else Py_False;
    ok = PyObject_IsTrue(x);
    if ok < 0 {
        return 0;
    }
    return PyBool_FromLong(ok);
}

def bool_new(type: PyTypeObject, args: PyObj, kwds: PyObj) -> PyObj {
    object: PyObj | None = None;
    if not _PyArg_NoKeywords("bool", kwds) {
        return 0;
    }
    if not _PyArg_CheckPositional("bool", PyTuple_GET_SIZE(args), 0, 1) {
        return 0;
    }
    if PyTuple_GET_SIZE(args) >= 1 {
        object = PyTuple_GET_ITEM(args, 0);
    }
    return bool_new_impl(type, object);
}

glob bool_new__doc__: str = "bool(object=False, /)\n--\n\nbool(object=False, /)\n\nReturns True when the argument is true, False otherwise.\n\nThe builtins True and False are the only two instances of the class bool.\nThe class bool is a subclass of the class int, and cannot be subclassed.";
