- **Refactor: Linear-time stale reactive-has check**: The W3041 `_check_stale_has_read` check now precomputes the earliest write position per name instead of scanning every read/write pair, reducing it from O(R x W) to O(R + W). Same diagnostics.
