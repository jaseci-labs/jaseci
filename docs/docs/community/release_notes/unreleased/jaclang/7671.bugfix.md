- **Fix: Desktop create/install bootstrap**: `jac create` now runs a full `jac install` (Python + npm) unless `--skip`, project venvs are created with the bundled CPython interpreter (not the fused `jac` binary), and the desktop launch script exports `JAC_DESKTOP_DEPS` so the embedded host can import serve dependencies installed at create time. Release payloads also stage `python/bin/python3.x` for venv creation.
