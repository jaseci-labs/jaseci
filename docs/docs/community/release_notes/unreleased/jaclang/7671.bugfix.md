- **Fix: Desktop create/install bootstrap**: `jac create` now runs a full `jac install` (Python + npm) unless `--skip`, and the desktop launch script exports `JAC_DESKTOP_DEPS` so the embedded host can import serve dependencies installed at create time. Under the fused `jac` binary, project-venv `pip install` uses `--target` into `.jac/venv` site-packages.
