- **Refactor: Module cache key probes**: `get_bytecode` now computes a module key at most once per fallthrough, reuses a single annex discovery for mtime and modkey checks, and memoizes the jaclang package version and per-path client kind lookups. Disk and hub caches now store structured `SEC_INPUTS` fingerprints so bytecode validity ignores test and style annex edits while still busting on impl, config, and compiler changes.
