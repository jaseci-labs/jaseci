- **Docs: edge abilities fire on edge visits, and the edge-reference algebra is now fully documented**: The OSP reference's "Known Limitation" box (and the node-edge skill's "silent no-op" pitfall) claimed edge entry/exit abilities never trigger; verified behavior is that they fire whenever the itinerary includes the edge itself (`visit [edge -->]` or a spawn on an edge), after which the walker continues to the target node - only plain node visits cross edges without waking them. The reference gains a Query Semantics section (per-reference dedup, edge-creation order, eager snapshot evaluation, bare-name edge-type slot with the variable workaround for runtime dispatch), Complex Queries now covers per-hop predicates in chains, mixed directions, ANDed predicates, and list anchors, and the deleting section shows typed-edge deletion by endpoint pinning. The tutorial gains a composed-reference example and the post-order-fold reading of exit abilities. The OSP skills pick up the same semantics plus two verified pitfalls: `+>:E:field=val:+>` silently default-constructs the edge, and untyped-predicate hops (`->::attr > 0:->`) are duck-typed and raise on `GenericEdge`.
