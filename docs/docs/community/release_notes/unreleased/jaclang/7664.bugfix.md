- **byLLM tool context propagation (#7664)**: tool calls now run in a copy of the caller's `contextvars` context. The worker pool and the `_call_sync` timeout thread previously dropped it, so tracing, request ids and cancellation signals silently read defaults inside tools.
