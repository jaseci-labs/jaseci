- **Fix: native RC allocator aborts cleanly on out-of-memory**: `__rc_alloc` and `__jac_halloc` now detect a `NULL` return from `malloc`, print `jac native: out of memory` to stderr, and call `abort()` instead of dereferencing the null pointer.
